---
slug: inspector-diff-workspace
date: "2026-09-09"
round: 15
verdict: CHANGES REQUESTED
feature_size: "M"
route: standard
branch: feat/inspector-diff-workspace
base: 7cd47b4
head: 805a32d + the uncommitted T40–T58 working tree
previous_review: review-2026-09-08-round14.md (CHANGES REQUESTED at 805a32d + the T40–T55 tree)
reviewers: 2 × sdd:reviewer (clean context, read-only, model opus) — stage 1 (claimed AC + full §4/§5 chain trace) and stage 2 (quality, edges, test adequacy of the changed surface). Each ran in its own git worktree with a symlinked node_modules over a detached 805a32d with the whole working patch applied. **Both sessions dropped mid-flight on `403 Request not allowed` and both were recovered and delivered complete** — see «Reviewer disclosure». The lead measured the gate in the main tree and ran its own 20-mutation labelled battery in a third worktree.
---

# Re-review round 15 — inspector-diff-workspace — 2026-09-09

## Scope

Fifteenth review, and the first over the **round-14 fix wave**: **T56** (criteria amendment),
**T57** (code fixes) and **T58** (records), all three **uncommitted** at review time, per the standing
rule that spec / SAD / task docs land in the same commit as the code they describe.

Whole feature diff `7cd47b4..HEAD` (**130 files, +12481 / −346**) plus the working tree
(**27 files, +1565 / −195**, six under `src/`: **+454 / −31** against `805a32d`) and 25 untracked files
under `docs/features/inspector-diff-workspace/`. **Changed surface since round 14:**

| task | what |
|---|---|
| **T56** | the canonical carve-out sentence carried to `sad.md:21` (§1 QG-1); the pointer `except where the header-cap guard binds (AC-18)` appended to the `List floor` invariant in `tasks/_epic.md`; `_epic.md:7` scoped to «on the right without stacked panels» (round-14 O1); a negation sweep re-run over nine files; T56–T58 registered in `tasks.json` / `_epic.md` / `tracker.md` |
| **T57** | **the first production change in five waves**: `main-content.spec.ts:388`'s stale `round(` → `floor(`, and `\|\| fileCount === 0` deleted from `listRows` in `inspector-layout.ts`, with the comment above `protectedList` rewritten to name the surviving arm as the only place the empty case is decided |
| **T58** | `round13-code-fixes.md`'s Outcome now names two unsatisfiable DoD bullets instead of one; T53's marker bullet qualified «three literally, two by adjacency» (round-14 O2); `test-plan.md:113`, `:120` and `:122` re-attributed to T57 — and a third carrier of the retired `round` found and fixed at `:113` by sweeping rather than by taking the finding's list |

Out of scope, unchanged since round 8: four working-tree modifications unrelated to this feature
(`.github/workflows/release.yml`, `.gitignore`, `CHANGELOG.md`, `install.sh`) plus an untracked
`.mcp.json`; `docs/plans/` is gitignored. `src-tauri` is untouched in the diff, in the working tree
and in `git status`, so the Rust gate is unchanged.

## Gate

Measured three ways — the lead in the main tree, stage 1 and stage 2 each in their own worktree — and
all three agree on every value.

`pnpm test` **839 tests / 63 files green**, exit 0 — the lead one run, stage 1 **three consecutive
serial runs identical**, stage 2 three (before its battery, after it, and again) · `pnpm lint` (biome,
**266 files**) clean, twice in each reviewer tree and before and after every mutation in the lead's ·
`tsc --noEmit -p tsconfig.spec.json` exit 0 · `pnpm build` clean, **908.38 kB** initial total in all
three trees, no budget warning · `git status --porcelain -- src-tauri` and
`git diff --name-only 7cd47b4..HEAD -- src-tauri` both empty. The one stderr line
(`[cdkFocusInitial]` not focusable) is the known jsdom noise. `fileParallelism: false` (T43) intact at
`vitest.config.ts:54`.

`git diff 805a32d -- src | grep -c '^-.*expect('` = **0**, and **0** over the whole feature diff: no
assertion removed, relaxed or emptied. No `it.skip` / `.only` / `.todo` / `.concurrent` anywhere in
`src/`. **Round-14 O9 did not recur**: zero `Test timed out in 5000ms` lines and zero «test module
already instantiated» errors across stage 2's 37 runs and stage 1's three, and stage 1 serialised its
own mutations against its measured runs so nothing of its own shared CPU with a measurement.

## Reviewer disclosure

**Both clean-context reviewers dropped mid-flight with `Please run /login · API Error: 403 Request not
allowed`, and both came back and delivered complete reports.** What that cost, and what it did not:

- **Stage 1** had finished its analysis when it dropped, and its first transmission was truncated
  inside R15-S1-F1. On request it re-sent everything in four messages: F2, F3+F4, F5 plus the tail of
  F1, and its eight observations with the gate close. It **could not write a report file** — the Write
  tool refuses report files for subagents, and a Bash heredoc failed on the combined length and
  quoting of its own prose (`unexpected EOF while looking for matching`). **Its findings are therefore
  transcribed into this record**, and its measurement scripts and logs remain reproducible at
  `C:/wt/r12s1/.r15s1/` (`extract2.py`, `sim2.js`, `equiv.js`, `verify.py`, `chain.py`, `mutate.sh`,
  `logs/`), copied to this session's scratchpad.
- **Stage 2** dropped after M24, and the loss was only the **identities** of its mutations:
  `battery.sh` took the mutation text as arguments and its `.apply` files recorded only
  `MUT_COUNT=1`. It recovered them **from its own transcript, not from memory**, corroborated each
  against the failing rows and messages in that run's own log, persisted them two ways
  (`.r15s2/identities.tsv` and an `identities.log` the harness now appends before applying), added six
  further runs (M26b, M27b, W1–W4) and delivered a **457-line report** at
  `C:/wt/r12s2/.r15s2/report.md`. Both are copied to this session's scratchpad.
- **Neither dying agent left a mutated file.** All seven baseline md5s in both worktrees are at their
  base values, `main-content.html` included (`d1fbdf2e…`); every one of stage 2's 34 runs reports
  `MD5_OK`; restores came only from each reviewer's own `pristine/` copies inside a
  `trap … EXIT INT TERM`, and no `git add` / `commit` / `checkout --` / `reset` / `clean` / `stash`
  ever ran in either worktree.
- **Stage 2's own disclosures**, recorded rather than paraphrased: round 14's `UnicodeEncodeError`
  recurred on it (on a `\u2265` out of `sad.md`) but during a **read-only** docs audit with no file
  mutated; and its first search for the 313-character sentence reported NOT FOUND through its own
  non-overlapping `re.finditer` bug, which it found and corrected **before** reporting.

So the independence the skill asks for is real this round — two genuine clean-context passes plus a
lead battery — and it took two recoveries to get there. **An earlier draft of this record said the
round's stage-2 coverage was too weak to have justified a `PASS`; that no longer holds and the
sentence is withdrawn.** What remains true is the process lesson in O11.

## What round 14 asked for, and got

**Every mechanical claim T56, T57 and T58 make reproduces** — each byte, count, attribution and
mutation, checked independently by at least two of the three agents. What blocks is not their
execution; it is what they propagated.

| round-14 finding | status | evidence |
|---|---|---|
| **R14-S1-F1** the two carriers phrasing the guarantee with *less* | **landed at both sites** | the canonical sentence is **one distinct value** across the **nine** artefact sites — `spec.md:198`, `spec.md:231`, `sad.md:21` (new), `sad.md:380`, `sad.md:733`, `screens.md:343`, `ux-flows.md:179`, `test-plan.md:91`, `test-plan.md:167` — plus three deliberate quotations in round-12/13/14 task files. Extracted by regex off disk and compared as byte strings by stage 1 and stage 2 (**313 characters** over the bold span, md5 `71e191bb259e30cb8285798a199677f3`) and by the lead over the inner span (311 characters, md5 `c0195724cd690d9b83f2dc16ac51c55e`) — different captures, same conclusion: one value, no drift. The pointer is likewise one distinct value, **47 characters**, md5 `c02eb85ec94f6385201a4f2de8f23d5e`, at `sad.md:368`, `ux-flows.md:170`, `ux-flows.md:219` and `_epic.md:215`. **But the sentence carried to nine sites is the wrong sentence at nine sites → R15-S1-F1 and R15-S1-F2**, and two live carriers outside every sweep's file list stay bare → **R15-S1-F3** |
| **R14-S2-F2** the stale `round(` | **closed at the site; the class had four carriers, not three** | `main-content.spec.ts:388` reads `floor(`; `grep -n "round(560"` returns nothing. T58 extended the sweep and found a third at `test-plan.md:113`. **A fourth survives at `DESIGN.md:345`, and T58's «three» is now asserted inside a live artefact → R15-S1-F4** |
| **R14-S2-F3** the vacuous `listRows` arm | **closed in the code, not in the test → R15-S2-F1** | the arm is gone; `fileCount === 0` survives once in code, at `protectedList:79`. Vacuity re-derived three ways with 0 differing outputs: stage 1 **720 360** configurations on T57's own grid and **2 521 260** extended to the 21 real token sets; stage 2 **720 360** exactly reproducing T57's figure; the lead **2 241 120** integer configurations plus **57 672** with fractional heights and stacked bases. Restoring the arm (**M15 ≡ L1**) leaves **839 green**. The mechanism that now carries the empty case is pinned hard: dropping `Math.min(fileCount, …)` (**L15**) is **10 failed / 829**. The operative sibling still reddens (**M12 ≡ C5 ≡ L2**): **3 failed / 836** across two tiers. **But «vacuous» is true only over the grids that were swept: stage 2 found the divergence class those grids structurally cannot contain, and it is reachable** |
| **R14-S2-F1** T54's unnamed unsatisfiable bullet | **closed for T54, reopened by T57** | `round13-code-fixes.md` now reads «could not be satisfied as written — **two**» and states what bullet 1 returns and why that is correct. **T57's own bullet 2 has the same defect and its Outcome says «none» → R15-S1-F5** |
| **O1** `_epic.md:7` unscoped | **closed** | reads «list floor of 50 % / 75 % **on the right without stacked panels**», with a dated marker explaining why it was scoped rather than given the pointer — **though that marker's own address is wrong → R15-S2-F2** |
| **O2** T53's marker bullet | **closed** | «three literally, two by adjacency», with the two mermaid labels named and their covering prose markers pointed at |

**Registration is clean**, re-run mechanically with three independent scripts: `tasks.json` parses,
**58 tasks**, 0 duplicate ids, 0 dangling deps, DFS finds no cycle, 58 task files, no id without a file
and no file without an entry, **0 `files_hint` mismatches over all 58**. `_epic.md:203-205` and
`tracker.md:63-65` mirror `T56 → T57`, `T57 → T58`, `T56 → T58`; `tracker.md:67` reads «Total: 58
tasks» and links the round-14 record. The `acs` ↔ Task-column check reproduces set for set:
**P1 = 12** (T2, T6, T7, T8, T12, T13, T21, T25, T29, T34, T35, T44), **P2 = 16** (P1 + T22, T23, T24,
T38), **T57 and T58 clean** (0 unattributed), **T56 excluded by the docs-task convention** of
`test-plan.md:29` exactly as T53 was in round 13 — which is what T58's Outcome says, in those words.
**T57's four quoted mutations C4 / C5 / C7 / C9 all land at the exact rows, counts and messages
recorded — four for four.**

**The chain, traced end to end** by stage 1 mechanically: all eight user stories US-01…US-08 have ≥ 1
AC and a row in the `sad.md:642-649` US → flow table; all **22** ACs have a §6 AC → flow row, a
`ux-flows.md` AC-map row, a `screens.md` mention and ≥ 1 coverage row over the 88-row table;
`target_surfaces: [desktop-app]`, one surface; every AC but **AC-20** has an automated component or
e2e-through-UI row (AC-20 manual by declaration, `test-plan.md:26`, no visual-regression pipeline). All
four ADRs are **Accepted** and cite ACs that exist. Nothing dropped out anywhere. The two
`added-by-fix` ACs were re-mutation-tested rather than taken on the record: AC-04's `protectedList`
arm → **3 failed / 836** across two tiers; AC-22's default flip → **3 failed / 836**, all three in
`preferences-schema.spec.ts`.

## Findings — stage 1 (spec / AC compliance)

| id | Finding | Resolution |
|---|---|---|
| **R15-S1-F1** | **The carve-out that four waves spent making byte-identical states a band narrower than the guard it describes, so AC-18 and AC-03 are still false in reachable configurations.** The sentence at all nine sites names two fixed pairs: «a remainder under 68 px comfortable / 60 px compact with the header expanded, or under 136 px / 120 px collapsed». Those four figures are the closed form `panelHeadH / (1 − ratio) − 1` evaluated at the **spec fixtures**. But `panelHeadH = Math.round(uiFontSize × 1.15 + 19 × padScale)` (`appearance-metrics.ts:78-98`), and the reachable input space is **21 pairs** — 3 densities (`preferences-schema.ts:5, 214`, `DENSITY_PAD_SCALE` 0.6 / 1 / 1.5 at `appearance-metrics.ts:41-45`) × `uiFontSize` 11…17 (`preferences-schema.ts:234-235, 440-443`). Over those 21 the collapsed band runs from **< 96 px** (compact/11) to **< 192 px** (relaxed/17). **Failure scenario:** density `relaxed`, `uiFontSize` 13, everything else default (`panelHeadH` 43, `fileRowH` 37), History, inspector at the bottom, splitter at a **272 px** column — legal, `MIN_BOTTOM_PX` is 220 (`main-content.ts:46`) — blame **and** file history stacked (`'0 0 30%'` + `'0 0 20%'`, `main-content.ts:270-276`) = a fixed **136 px** remainder, header collapsed → `protectedList = max(43 + 2×37, 0.75 × 136) = max(117, 102) = 117`, `headerAllowance = 19`, `headerMaxH = floor(max(19, 43)) = 43`, and the share `spec.md:229` defines is `(136 − 43) / 136 =` **0.683824** against **0.750**. The remainder is 136, i.e. **not** «under 136 px», so the carve-out does not apply and AC-18 plus spec §6 row 3 are unmet exactly as written. Swept over integer columns 220…1600 with both panels stacked: **688** configurations fall below 0.75 at a remainder at or above the figure the sentence gives their density, across **12 of the 21** token sets — compact/f17 n=8 (worst 0.741667); comfortable/f14 n=8 (0.742647), f15 n=16 (0.735294), f16 n=24 (0.727941), f17 n=40 (0.713235); relaxed/f11 n=56 (0.698529) through f17 n=112 (**0.647059**, the worst). The nine sets with no such miss are compact f11–f16 and comfortable f11–f13: **the band is correct for the one pair the fixture encodes and for nothing above it.** Measured independently and identically by stage 1 (`sim2-out.txt`) and the lead (`band2.js`), same 688. **The comfortable half fails without changing density** (fonts 14–17), so the finding stands whatever is decided about `relaxed`. **Why it blocks:** rounds 12, 13 and 14 each found an artefact stating the guarantee without its exception; the fix propagated an exception whose declared extent is narrower than the real guard's, so what remains is a bare assertion over the uncovered region — and the release engineer running spec §6 row 3's measurement at any non-default density logs a correct policy as a defect, word for word the scenario R14-S1-F1 described. The sentence now carried byte-for-byte to nine sites is the wrong sentence at nine sites instead of at one | **Fix now** → T59: restate the band as the closed form in `panelHeadH` («under `2 × panelHeadH − 1` expanded, under `4 × panelHeadH − 1` collapsed»), which is the guard the code implements and covers all 21 sets, byte-identical at the nine sites. **Owner decision:** `relaxed` is **in scope** — the artefacts move from «both densities» to the three the app ships |
| **R15-S1-F2** | **The compact half of every scoped figure, and the only acceptance instruction AC-20 has, rest on a token pair `computeMetrics` cannot produce.** `inspector-layout.spec.ts:14-19` declares `COMPACT_TOKENS = { fileRowH: 30, panelHeadH: 30 }`. The arithmetic the app ships is `height(basePad) = Math.round(uiFontSize × 1.15 + basePad × padScale)` with `PAD.fileRow = 15`, `PAD.panelHead = 19`, `padScale.compact = 0.6` (`appearance-metrics.ts:27-45, 80-85, 94`); compact at the default `uiFontSize` 13 gives **24 / 26**, and **no pair of the 21 gives 30 / 30** (compact by font: f11 22/24 … f17 29/31). The policy is fed the real values, not the fixture: `commit-inspector.ts:551-552` reads `this.rowHeight()` and `this.appearance.panelHeadHeight()`, and `styles.css:289-295` forbids a CSS density override and says in its own words that `--file-row-h: 30px` and `--panel-head-h: 34px` are «the defaults the service computes for 13px / comfortable». **Failure scenario A — AC-20.** `test-plan.md:95` is AC-20's **only** verification row (e2e-through-UI, manual) and reads «at compact density the summary line and list header are 30 px and file rows 30 px». The release engineer switches to compact at the default font, measures **26** and **24**, and either files a false defect against a correct policy or «fixes» it with the CSS density override `styles.css:289-292` forbids **because `--file-row-h` is the CDK's `itemSize`** — so the fix misplaces every row and drifts the branch-graph lanes off their commits. AC-20 is the one AC with no automated coverage, so that row is its entire verification. `screens.md:23` and `screens.md:345` carry the same figures. **Failure scenario B — AC-18.** The share **0.727273** is cited inside AC-18's marker (`spec.md:198`), spec §6 row 3's marker (`spec.md:231`) and **both markers T56 wrote in this wave** (`sad.md:21`, `tasks/_epic.md:215`). No test asserts it — the 220/110 collapsed twin at `inspector-layout.spec.ts:352-380` runs only with `COMFORTABLE_TOKENS`, and no compact 220/110 row exists — and it is **false**: with the real compact tokens that configuration gives `protectedList = max(26 + 48, 82.5) = 82.5`, allowance 27.5, `headerMaxH = floor(max(27.5, 26)) = 27`, share `(110 − 27) / 110 =` **0.754545**, which **meets** 0.750. The «compact twin» the branch has been pinning since round 11 does not exist in the shipped app. Re-derived by stage 1 and by the lead. **Measured, not argued:** substituting the real compact tokens for the fixture leaves **838 of 839 green** — only `inspector-layout.spec.ts:53` reddens (`expected 9 to be 7`) — so the compact half of the suite is insensitive to what the compact tokens are, and no test would have caught either branch of this. Secondary: `screens.md:23` pins `--file-row-h` to `FILE_ROW_HEIGHT`, a constant that **does not exist** in the repo (`grep -rn "FILE_ROW_HEIGHT" src` is empty; only `COMMIT_ROW_HEIGHT = 34` in `commit-list-layout.ts:4`), and calls invariant a token that scales with density. **Why it blocks:** it is the root cause of the compact half of F1, it leaves AC-20's only acceptance instruction unpassable against the shipped app, and it puts a measurably false figure inside two markers **this wave wrote** — the «wrong figure inside a record» class the branch has returned on five times, now with the wrong figure in the criterion as well | **Fix now** → T59 + T61: derive both fixtures from `computeMetrics` (compact/13 → 24/26, comfortable/13 → 30/34) instead of hard-coding them, re-derive `:53`, correct AC-20's row and `screens.md:23`, and replace the 0.727273 citations with the real counter-example |
| **R15-S1-F3** | **The canonical design system states the 75 % guarantee bare, twice, and `DESIGN.md` is in no sweep's file list.** `DESIGN.md:347-349`: «The policy's rule is that the **commit file list** keeps at least half of the column left by the stacked panels, and at least 75 % of it once the header is collapsed — collapsing the header is the developer asking for more list, so the stricter floor applies there.» No carve-out, no pointer, no placement scope — and it cannot be excused as right-placement-only, because it scopes *itself* to «the column left by the stacked panels», the remainder, which is the one case where it is false. `DESIGN.md:401`: «Commit file list \| `flex-1` — the only growing child in History, kept at half the column or more (75 % with the header collapsed)». A weaker third carrier folded in here rather than raised: `:353-354`, «so a commit with many refs cannot push the list under its share» — true of the yield mechanism, false of the cap guard, which is the one thing that does push it under. **Provenance:** this branch wrote **121 / 2** lines of `DESIGN.md` (`git diff --numstat 7cd47b4..HEAD -- DESIGN.md`) in `cd7f546` and `805a32d`, including both carriers and the «Inspector layout variables» and «The inspector column» sections entire; `tasks/_epic.md:210` cites `DESIGN.md §Density` as canonical in the epic's hard rules and `ARCHITECTURE.md:147` calls it «the design system (canonical)». **Failure scenario:** the reachable minimum — bottom placement at `MIN_BOTTOM_PX` 220, both panels stacked (110 px), header collapsed, comfortable → `protectedList = max(94, 82.5) = 94`, allowance 16, `headerMaxH = floor(max(16, 34)) = 34`, share **0.690909** against a document promising ≥ 0.750; **0.683824** at relaxed/13. Whoever reads the canonical design system, or the implementer told that this is the policy's rule, files a correct policy as a defect — and `DESIGN.md` is the first place either looks for `--inspector-header-max-h`. **Why it blocks:** fourth consecutive round of the class (R12-S1-F2 → R13-S1-F1 → R14-S1-F1 → here), and T56's sweep missed it **two independent ways**: its file list never included `DESIGN.md` (nor `adr/`, nor any root document), and its regex — the negations plus T53's four *share* nouns — matches neither «keeps at least half» nor «kept at half the column or more» even had the file been in scope. Round 14 told the wave to stop enumerating phrasings; the wave enumerated files | **Fix now** → T59, and redefine the sweep's scope as **every tracked file the branch touches** rather than an enumerated list |
| **R15-S1-F4** | **The fourth carrier of the retired `round`, and a false exhaustiveness claim now living in a live artefact.** `DESIGN.md:345`: `--inspector-header-max-h` is «whatever the commit file list's floor leaves of the column, **rounded to whole pixels** and never under one panel head». `inspector-layout.ts:90` is `Math.floor(Math.max(headerAllowance, panelHeadH))`, and has been since **T44** (round 10, R10-S2-F2). The line even paraphrases the code comment it contradicts — «never under one panel head» is `inspector-layout.ts:26`'s own wording — so it reads as a transcription of the policy. **The aggravation:** T58's Outcome (`round14-records.md:130-139`) and the marker it wrote into `test-plan.md:113` enumerate the class as **three** («So R14-S2-F2's class had **three** carriers, not one … and this row», and the live marker says «a **THIRD** carrier of it»). `DESIGN.md:345` is the fourth, so a false claim of exhaustiveness is now asserted in a shipped artefact and the next sweep of this class stops at three by citation. **Failure scenario:** R14-S2-F2's, one file over — someone reads the canonical design system, learns the cap rounds, restores `Math.round`, and at every odd column height the list loses its pixel and drops just under its 50 % floor: the 434 heights between 40 and 1000 px `inspector-layout.ts:85-89` quantifies. It fails loud (mutation **M11 ≡ L3**, floor → round, is **1 failed / 838**), and the document is still wrong. **Why it blocks:** it is the defect T58 was written to close, in the same wave, found by the method T58 itself prescribes («The finding's list is a starting point, never the enumeration») applied one file wider — and the enumeration T58 left behind is what will hide it | **Fix now** → T59 (the artefact) + T62 (the enumeration in the records) |
| **R15-S1-F5** | **A ticked DoD bullet that is measurably false, in the wave whose third task exists to fix exactly that.** `round14-code-fixes.md:99-101`, bullet 2, `[x]`: «`grep -c "fileCount === 0" src/app/core/services/inspector-layout.ts` **goes 2 → 1**, and the surviving one is at the `protectedList` arm». Measured: `grep -c` returns **2** — `:74`, inside the explanatory comment **T57 wrote in this same wave**, and `:79`, the `protectedList` arm. The count did not go 2 → 1; it went 2 → 2, with the second occurrence moving from code to prose. `:218-219` then closes «DoD bullets that could not be satisfied as written: **none**. The two that could have tripped were written against `805a32d` rather than `HEAD` precisely because of R14-S2-F1, and both hold as written» — while the Outcome's own prose at `:149-150` is carefully hedged («survives **once in code**»), so the true count was known when «none» was written. **Failure scenario:** literally R14-S2-F1's — an owner or the round-16 reviewer audits T57, runs the bullet's own command, gets `2`, and cannot tell from the task file whether the deletion landed, was reverted or was never applied. The substance is sound, so what fails is only the reporting — which is this wave's third task's entire subject. **Sixth consecutive round of the class** (R11 → R12-S1-F4 → R13 → R14-S2-F1 → here). Reached independently by stage 1, stage 2 (its O6) and the lead | **Fix now (record only)** → T62 |

## Findings — stage 2 (quality, edge cases, test adequacy)

| id | Finding | Resolution |
|---|---|---|
| **R15-S2-F1** | **The wave's only production change introduces a reachable `NaN`, and the caveat that was supposed to bound it states an identity its own expression refutes.** `round14-code-fixes.md:110-114` (DoD bullet 5, ticked) and `:200-205` record: «with `fileRowH = 0` the deleted arm was the only thing turning a `NaN` into `0` (`Math.floor(x/0) = NaN`, `Math.max(2, NaN) = NaN`, `Math.min(0, NaN) = NaN`)», declared «unreachable **by construction, not by convention**» — and the same unconditional claim is now in shipped code at `inspector-layout.ts:72-73`. Measured in node by stage 2 and by the lead: `Math.floor(280/0) = Infinity`, `Math.max(2, Infinity) = Infinity`, `Math.min(0, Infinity) =` **0**. The stated chain does not produce the stated result for any `listHeight` but exactly 0; of the 72 576 all-finite configurations with `fileRowH === 0`, **313 diverge and 72 263 do not**, so the real condition is `fileRowH === 0` **and** `listHeight === 0`. **And the caveat misses the reachable class entirely.** 18 of stage 2's 329 finite divergences need no `fileRowH = 0` at all, only `lineH === 0` with `headerAllowance === headerFixedH` — and `lineH === 0` is ordinary, because `commit-inspector.ts:559` falls back to `0` when `body` is absent and `#bodyText` lives inside `@if (commit.body.length > 0)` (`commit-inspector.html:154`), so **every commit with no message body gives `lineH = 0`**, passed into the policy unguarded at `:582` while `:561` guards `bodyLines` with the same `lineH > 0` test. Then `(headerAllowance − headerFixedH) / 0` is `0/0 = NaN` → `Math.max(1, NaN) = NaN` → `clampLines` NaN → `headerFixedH + NaN × 0` NaN → `listHeight` NaN → and **since T57 deleted the arm**, `Math.min(fileCount, Math.max(2, Math.floor(NaN / fileRowH)))` = **NaN** where the old code returned **0**. **Failure scenario, measured** (comfortable at `uiFontSize` 13 → `fileRowH` 30, `panelHeadH` 34; a body-less commit; an empty or fully-filtered file list; header expanded; no stacked panels; measured `headerFixedH` 94): at `availableHeight = 128` **exactly**, `listRows` goes from 0 to NaN (127 and 129 agree at 0), so `commit-inspector.ts:594` writes `--inspector-list-rows: NaN` and `commit-inspector.css:184`'s `height: calc(var(--inspector-list-rows, 6) * var(--file-row-h))` becomes invalid at computed-value time — **the `var()` fallback does not rescue it, because the property *is* set** — and the file list's height drops to `auto`. At the fixture's `headerFixedH` 112 the same height is 146; `headerFixedH` is measured off `header.scrollHeight`, so the affected height moves with the commit's ref badges. Pre-T57 the same input wrote only `--inspector-clamp-lines: NaN`, inert there because no body element exists to clamp. **Why it blocks:** not for the pixels — one height, empty list, and stage 2 says so plainly — but because this is the wave's only production change, the DoD bullet demands its caveat be recorded, and the caveat states a false identity, names a condition that is neither necessary nor sufficient, declares unreachable a configuration that is reachable, and rests on a 720 360-configuration sweep whose grid **provably cannot contain the counter-example** (it holds `lineH` at one positive value per token set) | **Fix now** → T60: pass `lineH` through the `lineH > 0` guard that already exists at `commit-inspector.ts:561` when building `tokens`, so the policy never receives a 0 — which closes the class, including the pre-existing `--inspector-clamp-lines: NaN` — with a row pinning the body-less commit |
| **R15-S2-F2** | **A record documents an address its own edit moved, and the delivered artefact now sends the reader there too.** `round14-criteria-amendment.md` cites `_epic.md:206` as the pointer's site in DoD bullet 2 (`:85`), DoD bullet 4 (`:98`), the Outcome's byte-identity list (`:133`) and «the cross-placement guarantee and its carve-out already live thirty lines below at `:206`» (`:152`) — and, worse, the **live marker T56 wrote at `_epic.md:7`** ends «the cross-placement guarantee and its carve-out live at :206». Measured: the 47-character pointer is at **`_epic.md:215`**; `sed -n '206p'` prints an **empty line**; `:203-205` are the T56–T58 registration rows T56 itself inserted, which pushed the invariants list down **nine** lines. The pointer's length, md5 and four carriers all hold. **Failure scenario:** an owner or the round-16 reviewer prints line 206, gets whitespace, and cannot tell whether the pointer landed, landed elsewhere or was reverted — the ambiguity R12-S1-F4 removed and R14-S2-F1 blocked on; and a reader following the epic's own goal line to «:206» lands on nothing. **Why it blocks:** a ticked bullet whose own command does not produce what it claims, unnamed in the Outcome, while that same Outcome *does* name a different unsatisfiable bullet — so the DoD reads as met. Self-inflicted inside the same edit. Reached independently by stage 1 (its O1), stage 2 and the lead | **Fix now (record only)** → T62: correct the address at the four citations and inside the `_epic.md:7` marker, or cite the invariant by its bold anchor rather than by line number |

## Findings — lead (test adequacy of the layout surface)

Raised by the lead and independently measured by stage 2, which classified all three as **observations**
(O1–O3) because `main-content.ts`'s only change in this wave is the comment at `:263-268` and
`main-content.html` is not in the wave's diff at all. **The owner chose to fix them now**; they are
recorded as findings so the decision is traceable, with their pre-existing status stated.

| id | Finding | Resolution |
|---|---|---|
| **R15-L-F1** | **The right-placement splitter clamp has no dimensional assertion at all, in either direction, while its bottom-placement twin has one.** `main-content.ts:37-38`: `MIN_CENTRE_PX = 360` → **60** leaves 839 green and → **2000** leaves 839 green; `MIN_RIGHT_PX = 320` → **20** and → **2000** likewise (stage 2's M26 / M26b / M27 / M27b; the lead's L16 / L17). Both are live: `:216` feeds `splitBounds().min` and `.other` for the right placement, consumed by `centrePx()` and `onWorkbenchResize()` (`:246-247`). The control that proves the bottom branch *is* covered is stage 2's **M25b** (`MIN_CENTRE_HEIGHT_PX` → 2000, **1 failed / 838**, `expected '2000px' to be '580px'`) and the lead's **L13** (`MIN_BOTTOM_PX` → 180, 1 / 838). **Failure scenario:** a maintainer widens `MIN_RIGHT_PX` to 480 to «stop the inspector getting unusable», the right-hand splitter silently refuses the width every user of a 1280 px window had, and the suite stays green — the shape of R9-S2-F1/F2. It also matters to the criteria: **T56's Outcome classifies three bare *expanded* carve-out hits as out of scope on the argument that «`MIN_RIGHT_PX` is a *width* (`main-content.ts:38`)»**, so a criteria-scope decision rests on a constant no test observes. The argument itself is sound — the lead confirmed the expanded band is unreachable in **all 21** token sets (largest expanded band 95 px at relaxed/17; smallest reachable remainder 110 px) — but its evidence is unpinned | **Fix now** → T61 |
| **R15-L-F2** | **Both stacked children can be halved inside their wrappers with nothing noticing.** `main-content.html:140` and `:154`: `<app-blame-viewer class="block h-full min-h-0">` → `h-1/2` leaves **839 green**, and the same on `<app-file-history-panel>` leaves **839 green** (stage 2's W3 / W4; the lead's L19 / L20). `h-full` is what makes each panel fill its fixed-basis wrapper, so `h-1/2` leaves half of a 30 % / 20 % band empty. The wrappers themselves are pinned three ways each — the flex basis (M21–M24 / L14, 1 / 838 each), `min-h-0` (W1 / W2, 1 / 838 each) and round 14's B1–B6 — so what is unpinned is exactly the child's fill of the slot its wrapper won: the AC-19 row asserts the wrapper's `classList` and inline style (`main-content.spec.ts:444-464`), never the inner host's class list, **which is assertable at this tier, as W1 / W2 prove** | **Fix now** → T61 |
| **R15-L-F3** | **`MIN_CENTRE_HEIGHT_PX`'s floor value is unpinned; only its upper-bound direction is witnessed.** `main-content.ts:213` → **100** leaves 839 green (stage 2's M25) while → **2000** reddens one row (M25b); the single row asserting the bottom-placement centre height expects `'580px'`, which a *lower* minimum never binds. So the constant is covered as an upper-bound witness only. Weakest of the three — the constant is observed, just not from below | **Fix now, riding along** → T61 |

## Checked and clean

**T57's deletion is behaviour-neutral over every grid that was swept**, three independent meshes
agreeing on 0 differing outputs, and the mechanism that carries the empty case after it —
`Math.min(fileCount, …)` — is the best-covered line in the function (**L15: 10 failed / 829**). What
R15-S2-F1 adds is that the grids could not contain the divergence class, not that the arithmetic was
misread.

**Mutation coverage across the three agents: 60 runs** — stage 2's 34 (28 redden, 8 green, every green
classified with proof), the lead's 20 labelled (15 redden) and stage 1's 6. Between them, every
constant of the policy, both row floors, both share floors, the head-cap guard, the flooring in both
directions, the `bodyLines` term, the share-floor ternary (swapped: **12 failed / 827**), the
`stackedPanelsHeight` subtraction, the `protectedList` empty arm, `Math.min(fileCount, …)`,
`MIN_BOTTOM_PX`, all four stacked bases and both wrappers' `min-h-0` **redden**. The greens are
accounted for: M15 ≡ L1 (behaviour-neutral by construction), M25 (R15-L-F3), M26/M26b/M27/M27b
(R15-L-F1) and W3/W4 (R15-L-F2). **No green run is unexplained**, which was not true of the recovered
partial battery earlier in the round.

**Conventions, boundaries, security.** biome clean over 266 files. T57's edit matches the file's idiom;
the `pnpm lint` failure T57 records (biome reformatting the shortened condition) is a real gate catch,
honestly reported. No new symbol, no `any`, no `as unknown as`, no non-null `!`, no `@ts-ignore` /
`@ts-expect-error` introduced anywhere in the wave. No new `invoke(`, `innerHTML`, `eval`, storage
access, IPC command, DOM sink, widened Tauri surface or secret. No new dead code — the wave *removed*
the only dead line the branch had. `main-content.html` is unchanged by this wave (md5 `d1fbdf2e…`).

**T58's own honesty is worth recording.** It names its shared-file DoD bullet as **not met** with the
reason, states that T56 is *excluded by predicate* rather than reporting it «clean», records that its
own `acs` was corrected away from what the round-14 record declared, and found the third `round`
carrier by extending the sweep instead of taking the finding's list. Three of this round's findings are
the species T58 was fixing; none of them is T58's.

## Observations — recorded, not findings

- **O1** `inspector-layout.spec.ts:353` calls the 220/110 collapsed row «the **one** REACHABLE
  configuration where AC-18's cross-placement guarantee yields». There are 688 below 0.75 outside the
  stated band alone, in 12 of 21 token sets, plus the whole in-band region. A false claim live in a
  spec comment; rides along with F1 rather than taking its own decision.
- **O2** `relaxed` is the third shipped density (`preferences-schema.ts:214`, offered at
  `settings-dialog.ts:123`, cycled at `palette-commands.ts:26`) and **no artefact of this feature
  mentions it**: spec §6 and AC-20 say «both densities», `test-plan.md` «both density token sets», and
  the suite has two fixtures. **Owner decision this round: `relaxed` is in scope** — the artefacts move
  to the three densities, and AC-20 gains its row. Folded into T59.
- **O3** `adr/0004-…md:58-59` and `:71` name the 50 % / 75 % figures with no carve-out and no placement
  scope, in an **Accepted** ADR that is part of the chain and in no sweep's file list. Stage 1
  classified it OUT-OF-SCOPE because the sentence refers «their measurement» to the spec rather than
  restating the guarantee — the pointer convention adopted in rounds 13 and 14 — and flagged it for an
  explicit decision. **Owner decision: add the pointer.** Folded into T59.
- **O4** The policy takes four measured pixel tokens with **no finiteness guard**:
  `commit-inspector.ts:561` guards `bodyLines` with `lineH > 0` but `:582` passes the same `lineH` in
  unguarded, and `:588`'s `Math.max(clampLines, 1)` propagates NaN rather than clamping it. This is
  R15-S2-F1's mechanism and is pre-existing; T60's fix closes it at the caller.
- **O5** `ARCHITECTURE.md:137-139` (in the feature diff, `5 / 0`) says «Row heights **are fixed by the
  density tokens in `styles.css`**», while `styles.css:294-295` says the opposite in its own words. It
  concerns `--row-h` rather than `--file-row-h` and is an architecture note rather than a criterion —
  but it is precisely the belief that makes F2's synthetic 30/30 plausible.
- **O6** The round-14 record states «every AC but AC-20 is named in at least two `it()` / `describe()`
  titles». Measured over 941 titles, **AC-19 appears in exactly one** (`main-content.spec.ts:411`; its
  other two occurrences, `:412` and `:477`, are comments). No cover is lost — AC-19 has a §6 row, a
  `ux-flows` row, a `screens` trace, four coverage rows and a real component test — only the round-14
  figure is wrong.
- **O7** `sad.md:21`'s new marker ends mid-sentence («Wording byte-identical to AC-18's -->»).
  Cosmetic; rides along with T59, which rewrites that marker anyway.
- **O8** `CHANGELOG.md:78-79` («capped at half the panel») describes the pre-reversal 50 % semantics in
  the release notes of an **already published** version and is outside the feature diff — historical
  record, not a live criterion, noted so a future sweep does not raise it again.
- **O9** Round-14 O4, O5, O7, O8, O11, O12 and O13 all stand unchanged; this wave touched none of
  them.
- **O10** The `acs` json ↔ task-file divergence T58 found stands and is not routed: `tasks.json`
  claims one AC the task file does not for **T44** (`AC-02`) and **T49** (`AC-01`). Confirmed by stage
  1, stage 2 and the lead. Bookkeeping, and which side is authoritative is an owner call — it belongs
  with the twelve P1 mismatches in the single later pass round-13 O15 and round-14 O12 reserved.
- **O11 — the process lesson of this round, worth more than any single finding.** Two reviewer sessions
  dropped on `403` and were recovered only because their measurements were on disk. **Dispatch
  reviewers with instructions to persist their report early to a path outside the repo** (the Write
  tool refuses report files for subagents and a long heredoc breaks on quoting — a short append loop
  works), and **require any mutation harness to log what it mutated before applying it**, not only the
  result. Stage 2 lost nothing permanent because it still had its identities in context; had it
  dropped one turn later, two green runs would have been unexplainable and this round could not have
  closed them.

## Verdict

**CHANGES REQUESTED.** Five stage-1 findings, two stage-2 findings and three lead findings. All ten
resolved with the owner as **Fix now**; nothing deferred, nothing dismissed. Routed to four tasks in
the branch's standing order:

| task | scope |
|---|---|
| **T59** — round-15 criteria amendment | **F1**: restate the carve-out band as the closed form in `panelHeadH` at the nine sites, byte-identical · **O2**: the three shipped densities replace «both densities» in spec §6, AC-20 and the test plan · **F2 (artefact half)**: correct AC-20's row (`test-plan.md:95`), `screens.md:23` (the token the policy reads, not the non-existent `FILE_ROW_HEIGHT`) and `screens.md:345`, and replace the four `0.727273` citations with the real counter-example · **F3 + F4**: `DESIGN.md:345`, `:347-349`, `:353-354`, `:401` · **O3**: the pointer on ADR-0004 `:58-59` and `:71` · **O7** · redefine the sweep's scope as every tracked file the branch touches and re-run it under that scope · register T59–T62 |
| **T60** — round-15 production fix | **R15-S2-F1**: pass `lineH` through the existing `lineH > 0` guard when building `tokens` at `commit-inspector.ts:559-582`, with a row pinning the body-less commit at the divergent height, so `--inspector-list-rows` can never be written `NaN` (closes **O4** too) |
| **T61** — round-15 test coverage | **F2 (fixture half)**: derive both fixtures from `computeMetrics` and re-derive `inspector-layout.spec.ts:53` · **R15-L-F1**: a component-tier row observing the right-placement clamp in both directions · **R15-L-F2**: pin `h-full` on both inner panel hosts · **R15-L-F3**: witness `MIN_CENTRE_HEIGHT_PX` from below |
| **T62** — round-15 records | **F5**: re-word T57's bullet 2 as «goes from two occurrences in code to one, the `protectedList` arm; `grep -c` still returns 2 because the round-14 comment quotes the retired condition» and name it in the Outcome · **R15-S2-F2**: correct `_epic.md:206` → `:215` at the four citations and inside the `_epic.md:7` marker · **F4 (record half)**: correct the «three carriers» enumeration in `round14-records.md:130-139` and the live marker at `test-plan.md:113` to four |

No open stage-1 finding remains unresolved. **T60 is the first production fix this branch has taken
from a review since T44**, and it is the one item here that changes shipped behaviour.
