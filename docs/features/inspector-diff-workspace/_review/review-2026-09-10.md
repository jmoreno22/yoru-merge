---
slug: inspector-diff-workspace
date: "2026-09-10"
round: 16
verdict: CHANGES REQUESTED
feature_size: "M"
route: standard
branch: feat/inspector-diff-workspace
base: 7cd47b4
head: 805a32d + the uncommitted T59–T62 working tree
previous_review: review-2026-09-09.md (CHANGES REQUESTED at 805a32d + the T40–T58 tree)
reviewers: 2 × sdd:reviewer (clean context, read-only, model opus) — stage 1 (claimed AC + full §4/§5 chain trace) and stage 2 (quality, edges, test adequacy of the changed surface). Each ran in its own git worktree with a junctioned `node_modules` over a detached `805a32d` with the whole working patch applied and the 31 untracked files copied in. Both were dispatched with the round-15 **O11** discipline — persist the report outside the repo before analysing, log every mutation before applying it — and both delivered complete; the two truncations were in message transit, recovered from the persisted files. The lead measured the gate in the main tree and ran its own arithmetic, chain and sweep batteries.
---

# Re-review round 16 — inspector-diff-workspace — 2026-09-10

## Scope

Sixteenth review, and the first over the **round-15 fix wave**: **T59** (criteria amendment),
**T60** (the production fix), **T61** (test coverage) and **T62** (records), all four **uncommitted**
at review time, per the standing rule that spec / SAD / task docs land in the same commit as the code
they describe.

Whole feature diff `7cd47b4..805a32d` (**64 files under `src/`, +7248 / −327**) plus the working tree
(**8 files under `src/` and `vitest.config.ts`, +638 / −51** against `805a32d`) and 31 untracked files.
**Changed surface since round 15:**

| task | what |
|---|---|
| **T59** | the carve-out band restated as a closed form in `--panel-head-h`, byte-identical at ten artefact sites; `DESIGN.md` `:345`/`:347-349`/`:353-354`/`:408` and ADR-0004 `:58-59`/`:72` corrected; AC-20's row, `screens.md:23` and `:345` re-pointed at what `computeMetrics` derives; the `0.727273` markers replaced; the sweep re-scoped to every tracked file the branch touches; T59–T62 registered |
| **T60** | **the only production change**: `lineH` passed through the `lineH > 0` guard when building `tokens` (`commit-inspector.ts:591`), with a component-tier row pinning a body-less commit |
| **T61** | both fixtures derived from `computeMetrics` and a third (`relaxed`) added; `:53` re-derived 7 → 9; the AC-18 bottom row given per-density maps; witnesses for `MIN_CENTRE_PX`, `MIN_RIGHT_PX`, `MIN_CENTRE_HEIGHT_PX` in both directions and for `h-full` on both stacked hosts |
| **T62** | T57's grep bullet re-worded to what the command returns; the `_epic.md` citations moved to the bold anchor; the «three carriers» enumeration corrected to four |

## Gate

Measured in the main tree by the lead and independently in each reviewer's worktree — three
agreeing measurements:

- `pnpm test` → **841 passed (841), 63 files**. Stage 2 ran the suite **40 times** through the
  battery and every run reported exactly 841 / 63, with failures confined to each mutation's own
  target: independent confirmation that `vitest.config.ts`'s new `fileParallelism: false` does what
  its comment claims, and that the flakiness T43 was written for is gone.
- `pnpm lint` (biome) → **clean over 266 files**, measured four times.
- `tsc --noEmit -p tsconfig.spec.json` → exit 0 (stage 2).
- `git status --porcelain -- src-tauri` empty; the Rust half is untouched by the whole feature diff.

## Reviewer disclosure

**Both reviewer messages truncated in transit**, at different points, and both were recovered in
full from the persisted report files the dispatch required — the round-15 **O11** discipline paying
for itself on its first outing. Stage 1's report is 485 lines, stage 2's 505. No verdict in this
record rests on a message whose text was not read.

**Stage 1 corrupted a file mid-battery and disclosed it.** `sed -i` on `commit-inspector.ts` produced
`SyntaxError: Invalid or unexpected token` and no test ran; the file was restored from a pristine copy
with its md5 re-checked before anything else executed, and the mutation re-applied through Python with
`newline=''`. Recorded because a battery that hides a failed application is worth nothing.

**Every mutated file was restored and md5-verified against a backup taken before the battery**, in
both worktrees and by the lead in the main tree:

```
inspector-layout.ts       f10fa099c0981638022f412395001db6
appearance-metrics.ts     f50d48f3108168abaf083311f6815eba
commit-inspector.ts       44f017a4c415d7a1c001f7e0362d6a9c
main-content.ts           720244ba837d5fa7229ea935bb6d4704
main-content.html         d1fbdf2ef79301507843b0053f8b6586
```

No `git add` / `commit` / `checkout --` / `reset` / `clean` / `stash` ran in any worktree.

## What round 15 asked for, and got

| round-15 item | status |
|---|---|
| **R15-S1-F1** the band narrower than the guard | **closed for what the criteria promise → R16-S2-F1 for what the words say.** The restated sentence is one distinct value at **10** artefact sites, 300 chars, md5 `c9c64c1ea1332557710af4baacbda9a5` — enumerated independently by stage 1 and the lead, no 11th site. Re-swept independently twice over 21 token sets × 2 collapse states × remainders 1…1200 at 0.25 px = **201 474 points, 0 mismatches** against the share-yield predicate, T59's headline figure to the point |
| **R15-S1-F2** the unreachable `{30, 30}` fixture | **fixture half closed, prose half not → R16-S1-F1, F2, F3.** `tokensFor()` calls `computeMetrics`; the three pairs (26/24, 34/30, 43/37) verified against the generator by all three agents; AC-20's row is now passable. **And the derivation is genuinely coupled, not self-fulfilling**: mutating `computeMetrics` itself reddens 7 (`PAD.fileRow`), 8 (`PAD.panelHead`) and 14 rows (`TEXT_LINE_RATIO`) across up to three spec files, `inspector-layout.spec.ts` among them every time (6 rows under each of the first two). The evidence is the **literal-anchored** assertions, not the derivation-anchored ones — see **O15**; three clean witnesses, each hand-re-derived: `:74`'s `toBe(9)` (the very literal T61 re-derived from 7 — with `PAD.fileRow` 20, compact `fileRowH` goes 24 → 27 and `(420 − 168 − 26) / 27 = 8.37 → 8`), `:169`'s `toBe(606)`, and the `56` in the squeezed AC-03 row. All three reddened under all three mutations |
| **R15-S1-F3 / F4** `DESIGN.md`, ADR-0004, the sweep's scope | **closed for those files; the class survives elsewhere → R16-S1-F2, F7, L-F1.** `DESIGN.md:345` now reads «floored», `:347-349` states the exception with the reachable counter-example, `:408` takes the pointer; ADR-0004 carries it at `:58-59` and `:72`. Stage 1's own meaning-level re-sweep over the 171-file union found no bare *cross-placement* carrier left, and confirmed by measurement that the surviving bare **expanded** statements are safe (largest expanded band 96 px at relaxed/17; smallest reachable remainder 110 px) |
| **R15-S1-F5** T57's false grep bullet | **closed.** `grep -c` returns **2** at `:74` and `:79`, byte-for-byte what the re-worded bullet and the pasted output say; the Outcome's «none» became «one, named 2026-09-09 by T62». The transferable rule is written down: *a bullet whose subject is a count states what the command returns, never what the change did to the code* |
| **R15-S2-F1** the reachable `NaN` | **closed, and the caveat is right this time.** `commit-inspector.ts:591` reads `lineH: lineH > 0 ? lineH : 1`. Three independent sweeps with three different grids — the lead's 571 722 non-finite → 0, stage 2's **14 772 384 configurations** (grid built around the three algebraic points where `headerAllowance == headerFixedH`) 174 540 → **0**, stage 1's analytic derivation — agree. **All eight claims of the caveat T60 wrote into shipped code verified true**, including that `calc(NaN * 30px)` is invalid at computed-value time and that the `var()` fallback cannot rescue a property that *is* set. Mutation **T60a** (guard removed): **1 failed / 840**; **T60b** (guard removed and the rows-side assertions neutralised): **1 failed / 840** at `commit-inspector.spec.ts:634:44` — both halves of the pinning row are load-bearing. The row derives its height (`3 × panelHeadH` + `panelHeadH`) instead of hard-coding 128, so it follows the class |
| **R15-S2-F2** the moved address | **not closed → R16-S1-F4.** The citations are now by bold anchor, which is the right fix; the addresses reported around them are false again |
| **R15-L-F1** right-placement clamp unobserved | **closed.** `MIN_RIGHT_PX` → 20 and → 2000 each redden one row (`'1024px'` / `'360px'` against `'960px'`); `MIN_CENTRE_PX` likewise. Both clamps witnessed in both directions, which neither was at any value from 20 to 2000 |
| **R15-L-F2** both stacked children halvable | **closed.** `h-full` → `h-1/2` reddens on `<app-blame-viewer>` and on `<app-file-history-panel>`, each with its own labelled witness; all four declared flex bases pinned |
| **R15-L-F3** `MIN_CENTRE_HEIGHT_PX` unwitnessed from below | **closed.** → 100 reddens 1, → 2000 reddens 2 |
| **O2** `relaxed` in scope | **partly delivered → R16-S1-F5.** Landed at `sad.md:40`, `test-plan.md:95` and `:99`'s label; thirteen live sites still say «both densities» |
| **O1** the «one REACHABLE configuration» claim | **not delivered → R16-L-F2** |
| **O3** ADR-0004 bare · **O4** no finiteness guard · **O7** `sad.md:21`'s truncated marker | **all closed.** O4 is now *verified unreachable* rather than merely unexercised: `uiDensity` is validated by `isOneOf` (`preferences-schema.ts:304-305`) and `uiFontSize` is clamped on both the parse and the setter path, so `fileRowH ≥ 22` and `panelHeadH ≥ 24` for every reachable preference |

## Findings — stage 1 (spec / AC compliance)

| id | Finding | Resolution |
|---|---|---|
| **R16-S1-F1** | **UNMET AC-18 (and AC-01 / AC-02 at `:90`): the three cells a tester executes still carry the dead fixture's shares, and one of them states an identity that holds in only one of the three shipped densities.** `test-plan.md:91` is AC-18's manual row — «the fourth run of the §NFR bullet below is that case and expects **0.691 comfortable / 0.727 compact**, not 0.750»; `:167` is that run, same figures, same «not the 0.750»; `:90` is the automated row — «at the reachable 220 / 110 remainder **the cap is one panel head and the share is 0.6909** … **expanded and collapsed alike**, because the 2-row floor binds there and the two states are numerically identical». Measured at remainder 110 with the default font, independently by stage 1 and the lead: comfortable exp/col cap **34 / 34**, share **0.690909** both, cap *is* one head · compact exp cap **36** share 0.672727, col cap **27** share **0.754545**, cap is **not** one head and the collapsed share **meets** 0.750 because `4 × 26 = 104 < 110` so the guard never binds · relaxed exp/col cap **43 / 43**, share **0.609091**. So all three claims of `:90` are false at compact, and its figure is false at relaxed; `0.727273` is reachable only at compact / `uiFontSize` **16**. **Failure scenario:** the release engineer executes `:167`'s fourth prescribed run at compact and the default font, measures **0.754545**, is told to expect 0.727 and explicitly «not the 0.750», and either files a defect against a correct policy or records a pass for a figure the app never produces; at relaxed the same run measures 0.609091 and the plan gives no figure at all. **Why it blocks:** R15-S1-F2's failure scenario A verbatim, one file over, in the file T59 owns — T59 scoped its fix to the six-digit literal («it appears in two markers») and never swept the three-digit form in live prose | **Fix now** → T63 |
| **R16-S1-F2** | **UNMET AC-01 / AC-02: the retired fixture band survives in `src/` as a dated instruction to restore an escape hatch, with seven stale figures and no `relaxed` column.** `inspector-layout.spec.ts:547-572`, the comment above the share sweep whose loop **T61 widened to three densities in this wave**. Measured against the fixtures `tokensFor()` now derives: `:548` «compact expanded `h ≤ 59` collapsed `h ≤ 119`» → really **51 / 103** · `:553`'s closed forms «`30`/0.5−1, `30`/0.25−1» → compact `panelHeadH` is **26** · `:555` «**224** missing configurations across **both densities**» → **200** over the two named, **378** over the three the loop now sweeps · `:559-560` «below **120 px** compact (**90** > 0.75h)» → below **99 px**, row floor **74** · `:567` «h ≤ **179** compact expanded and h ≤ **359** collapsed» → **147 / 295** · `:572` «**Restore it, deliberately, only below the bound that applies — 68 / 60 px expanded, 136 / 120 px collapsed**» → compact **52 / 104** · and `relaxed` is absent entirely (expanded 85, collapsed 171, row-floor bound 156, restore bound 86 / 172). Every comfortable figure in the block is correct; every compact one is the unreachable `{30, 30}` fixture's, **fifteen lines below the comment T61 wrote in this wave to record that that fixture was the defect**. **Failure scenario:** `:572` is an explicit instruction, not a stale note. A maintainer who follows it in compact restores the excuse predicate up to 60 px expanded where the real bound is 52, waiving the ≥ 50 % guarantee across `h = 52…60` where the policy meets it, and up to 120 px collapsed where the real bound is 104 — byte-for-byte the defect R12-S1-F1 fixed once, the same 136 / 120 numbers surviving as the compact column. In relaxed, whose bounds the block never gives, the same maintainer under-restores and leaves 46 expanded plus 132 collapsed heights failing the sweep with no stated reason. **Why it blocks:** it is R15-S1-F2's defect alive in the file whose de-fixturing was T61's entire remit, after T61 ran; T59 swept 171 tracked **doc** files and T61 swept the **tokens**, and neither swept the spec's prose. Fifth consecutive round of the class, first time inside `src/`. Found independently by stage 1 (F2), stage 2 (F3, with the full per-density table) and the lead | **Fix now** → T64, replacing the per-density figures with the closed forms per the owner's anti-recurrence decision |
| **R16-S1-F3** | **UNMET AC-01 / AC-02 verification breadth: four precise counts falsified by this wave's own code change, in the rows that state what those ACs are swept over.** `test-plan.md:35` (AC-01, automated) «swept over **192 configurations** (8 heights × 6 file counts × 2 collapse states × **2 density token sets**)» · `:99` (AC-02) «in the **three** density sets and … swept over **192 configurations**» — the label was updated and the arithmetic was not · `:145-151`, the block that exists so those counts can be audited: «the **share** sweep … is **192**», «the whole-pixel cap row … is **384**», «the **32** `0`-file configurations». Measured against `inspector-layout.spec.ts:513-586`, which now iterates `DENSITIES` (3 entries, `:30`) × 8 heights (`:475`) × 6 file counts (`:476`) × 2 collapse states: share sweep **288**, cap row **576**, `0`-file configurations **48**. **Failure scenario:** the owner or the next reviewer audits AC-01's breadth claim, cannot reconcile 192 with the code, and cannot tell whether the sweep shrank, a density was dropped or the prose drifted — which is R10-S1-F10 and round-11 O9, the two rounds this very block was written to close. **Why it blocks:** `test-plan.md` is in T59's `files_hint`, and T59 ran **before** T61 widened the loop, so no bullet in the wave could catch it — the ordering hazard, not carelessness | **Fix now** → T63, with the counts expressed as the loop's own arithmetic rather than as literals |
| **R16-S1-F4** | **A ticked DoD bullet whose own two commands contradict the Outcome, in the task written to close exactly that — and the address moved a second time inside the same wave.** `round15-records.md:115-116`: «Verified both ways — `sed -n '206p'` prints an **empty line**, and the 47-character pointer greps to **`:215`**». Measured: `sed -n '206p'` on `_epic.md` prints `| T51 | [Round-12 code fixes](…) | domain | T50 | …`, and the pointer is at **`:227`** — T59's own T59–T62 registration rows pushed the invariants list down another **twelve** lines, exactly as T56's had pushed it nine. The stale address also lives in `round14-criteria-amendment.md:133`, one of the four citations T62 *corrected* in this wave, whose parenthesis still reads «`:215` after this wave's own registration rows moved it down nine lines» — a stale address and a stale delta inside the correction itself. T62's Outcome then reads «DoD bullets that could not be satisfied as written: **none**». **Failure scenario:** literally R14-S2-F1's and R15-S2-F2's — the round-17 reviewer runs the bullet's own commands, gets a registration row where the record promises whitespace, and cannot tell whether the correction landed, landed elsewhere or was reverted. That is what happened this round. **Why it blocks:** **seventh consecutive round of the class, third occurrence on this one line**, self-inflicted in the task written to close it. The bold-anchor citation itself is correct and is the right fix — T59 adopted the rule and T62 wrote a line number anyway, three lines under its own sentence recommending the anchor. Reached independently by stage 1 and the lead | **Fix now (record only)** → T66, citing by anchor everywhere including inside explanatory parentheses |
| **R16-S1-F5** | **UNMET AC-01 / AC-02 / AC-03 / AC-18 / AC-20: commissioned scope, named site-by-site in the round-15 verdict, delivered at three sites of sixteen — and the density it was commissioned for is the one no measurement instruction reaches.** The round-15 verdict routed **O2** to T59 as «the three shipped densities replace «both densities» in **spec §6**, AC-20 **and the test plan**». Landed: `sad.md:40`, `test-plan.md:95` (AC-20), `:99`'s label. **Not landed:** `spec.md:229` — §6's measurement clause itself, «measured at 960 × 640 and 1280 × 800 **in both densities**», which row 2 inherits via «same» · `sad.md:734` — §10 QG-1's *How verify* · `sad.md:690` — the release-time measurements · `sad.md:742` — QG-3's *When*, «both themes × **both densities** × both placements» · `test-plan.md:38`, `:43` (twice), `:47`, `:90`, `:116`, `:165` («**4 runs** … × both densities»), `:179` (the pre-release checklist). **Failure scenario:** the release engineer follows §6, §10 QG-1 and the checklist, runs two densities, and never visits `relaxed` — the density with the widest guard band and the worst reachable share, **0.647059** at relaxed/17 column 272, re-derived independently by stage 1 and the lead. The configurations the owner decision was taken *for* are the ones no instruction reaches. One file also contradicts itself: `:95` says three densities, `:38` and `:43` say two, and `:165`'s «4 runs» is 6 under the decision. **Why it blocks:** T61 widened the automated half, so the gap is entirely in the manual half — which is AC-20's whole verification — and T59's Outcome reports «none» unsatisfiable rather than naming it | **Fix now** → T63 |
| **R16-S1-F6** | **T62 nested an HTML comment, so marker prose and a stray `-->` now render inside a live coverage row.** `test-plan.md:113`, the AC-03 / AC-04 / AC-06 row. The cell's marker opens `<!-- re-pointed 2026-09-08 …` and T62 inserted a second `<!-- corrected 2026-09-09 (T62, review round 15 R15-S1-F4): … -->` **inside** it. HTML comments do not nest: the outer comment terminates at the inner `-->`, so the tail renders as visible table-cell content — « Flooring can only give the list more; rounding cost it a pixel at 434 of the heights between 40 and 1000 px expanded -->». Token-scanned by stage 1 and by the lead over **all 171** markdown files the branch touches: this is the **only** nested or unbalanced case in the branch, and it is new in this wave. **Failure scenario:** anyone reading the rendered test plan — the release engineer working the manual rows, or the next reviewer — sees a fragment of review provenance and a raw comment terminator inside a coverage row's expected-result cell, while the other half of the same marker stays hidden. **Why it blocks:** a delivered artefact defect created by this wave, in the row T62 was editing, reported as landed. Low blast radius; cited and self-inflicted | **Fix now** → T66 |
| **R16-S1-F7** | **The enumeration T62 was written to correct is incoherent, omits the carrier it names, cites the wrong address, does not name the scope it swept — and the count is already a floor again, in `src/`.** `round14-records.md:130-139` now reads «So R14-S2-F2's class had **four** carriers, not one — three of them known when this was written and the fourth found by round 15 (R15-S1-F4): `main-content.spec.ts:388` …, `inspector-layout.ts:80-83` (**which was always right** and explains the choice) and this row.» Three items, one of them explicitly *not* a carrier, and `DESIGN.md:345` — the actual fourth, which the same sentence names as the round-15 find — is never in the list. `inspector-layout.ts:80-83` is also the wrong address: the flooring comment is `:84-89` and the operation `:90`. **Neither** this passage **nor** the live marker at `test-plan.md:113` names the scope it swept, which is the rule T62 wrote into both in the same edit («an enumeration of a defect class is a floor, never a total, and it must name the scope it swept»). And the count is a floor again: `inspector-layout.spec.ts:590`'s test title reads «**rounds** the cap to whole pixels when the measured inputs carry fractions (R9)» while `inspector-layout.ts:90` has been `Math.floor` since T44 — a **fifth** carrier, in the spec that owns the policy. **Failure scenario:** R15-S1-F4's — a maintainer reading the titles of the policy's own spec learns the cap rounds, restores `Math.round`, and takes the list's pixel at 434 of the heights between 40 and 1000 px. It fails loud (mutation **L7**: 1 failed / 840), and the next sweep of this class stops at «four» by citation while the fifth sits in `src/`. **Why it blocks:** the count is the thing T62 was written to fix, wrong in the same two places, in the same shape, one round later | **Fix now** → T64 (the test title) + T66 (the enumeration, with its scope stated) |

## Findings — stage 2 (quality, edge cases, test adequacy)

| id | Finding | Resolution |
|---|---|---|
| **R16-S2-F1** | **The canonical sentence names the wrong predicate, and T59's 0-mismatch certification measures a different quantity than the words it certifies.** The 300-character sentence at all ten sites reads «except where **the header-cap guard binds** — a remainder under twice `--panel-head-h` with the header expanded, or under four times it collapsed», and `spec.md:198`'s marker certifies «**the guard binds** exactly for `r < 2 × panelHeadH` expanded and `r < 4 × panelHeadH` collapsed — verified at 201 474 points, 0 mismatches». Measured on the shipped policy over that same grid, separating the two quantities the sentence fuses — independently by stage 2 and re-derived by the lead, identical numbers: `predicate ≠ shareMissed` = **0** · `predicate ≠ guardBinds` = **5312**, **every one of them expanded**. The cause is algebraic: when the list's 2-row floor sets `protectedList` (`panelHeadH + 2·fileRowH > 0.5r`), the guard binds up to `r < 2·panelHeadH + 2·fileRowH` — a band `2 × fileRowH` wider than the sentence's. **16 of the 21 token sets have a reachable gap** (comfortable/13 `r = 110…127.75`, relaxed/17 `r = 110…179.75`). **And it contradicts the branch's most-cited row, in the same wave:** `inspector-layout.spec.ts:340-343` says «headerAllowance = 110 − max(94, 55) = 16, so the `panelHeadH` guard lifts the cap to 34» — measured true — while `spec.md:198` says the guard binds only under 68. Two live artefacts of this feature give opposite answers about the one configuration every carve-out on this branch was written for. **Failure scenario:** a maintainer reads `spec.md:198`, concludes the guard is dead above `2 × --panel-head-h`, and narrows `Math.max(headerAllowance, panelHeadH)` to that band as a tidy-up. At the reachable 220 / 110 bottom placement, comfortable, the expanded header is then capped at 16 px against a 34 px panel head and disappears behind its own scrollbar — the regression AC-03's guard exists to prevent. The suite catches the code change; the doc is what is wrong, and the doc is what a maintainer reads first. **Why it blocks:** fifth consecutive round of «a marker states an identity its own expression refutes», and it is the **same distinction round 12 already litigated** — R12-S1-F1 separated «the guard binds» from «the share is missed» and fixed a wording that conflated them; T59 re-fused them and certified the result with a point count. Nothing behavioural: no code is wrong and **no criterion can be falsified by it**, because where the guard binds outside the band the share is 0.6091…0.6909 against a 0.50 floor | **Fix now** → T63. **Owner decision:** reword the predicate at all ten sites — «except where the header-cap guard **costs the list its ratio**» — and restate T59's certification as a measurement of the share-yield predicate, which is exactly what it measured. Leaving the two readings standing is what produced round 12 and this round on the same distinction |
| **R16-S2-F2** | **The clamp term's rounding direction is the third rounding site in the same function and the only one nothing observes — and `round` there is wrong, not merely different.** `inspector-layout.ts:99`, `Math.floor((headerAllowance − headerFixedH) / lineH)` inside the `clampLines` `Math.min` (AC-03: the body clamp yields first, down to one line). **Measured, labelled: L8 · `Math.floor` → `Math.round` · 841 passed (841) — the only green run in a 39-mutation battery.** It is not behaviour-neutral: reproducing the shipped expression both ways, the grid the suite's broadest rows visit shows no divergence (which is why L8 is green), while the reachable grid (3 densities × `h = 40…1200` × file counts × body lines) diverges at **1215** configurations — re-derived by the lead at the same 1215. Witness, verified twice: comfortable/13, `availableHeight` 278, `fileCount` 30, `bodyLines ≥ 2` → floor `clampLines` **1**, round **2**. Round is wrong because `q = 1.556` lines fit: `headerHeight` becomes `112 + 2×18 = 148` against an allowance of **139**, so `Math.min(headerHeight, headerMaxH)` clips it and the expanded header goes into its own scrollbar with a half-cut body line — and because `measureBody()` compares the body's `scrollHeight` to its `clientHeight`, a clamp one line too generous also stops «Show more» appearing on bodies that *are* being cut (`commit-inspector.ts:478-483`, AC-01). The list is protected — `headerMaxH` bounds the damage and `listRows` is unchanged — which is why nothing else notices. **Why it blocks:** the other two rounding sites in this function are pinned *precisely because a reviewer found them* — R10-S2-F2 changed `headerMaxH`'s `Math.round` to `Math.floor` and round 10 pinned it with height 421; R11-S2-F1 found the collapsed half unwatched and pinned it with 422. Both live in the heights table's comment at `inspector-layout.spec.ts:466-474`, which explains the mod-4 classes for *those two* quotients and says nothing about this one; the two rows the table feeds assert `headerMaxH` and the share and **never `clampLines`**, so the table cannot observe this site by construction — the same structural blindness R15-S2-F1 named for `lineH`. Mutation **L7** reddens 1 / 840, so the sibling is covered and this one is not | **Fix now** → T64. **Owner decision:** one unit row derived from the fixture, at a height where the quotient's fractional part is ≥ 0.5 inside the `[1, MAX_CLAMP_LINES)` window, asserting `clampLines`. Derived, not pinned at 278 |

## Findings — lead

| id | Finding | Resolution |
|---|---|---|
| **R16-L-F1** | **The SAD pins a live technical constraint to `FILE_ROW_HEIGHT`, a constant that does not exist — and the marker T59 wrote one file over states that fact in its own words.** `sad.md:38`: «Every fixed dimension comes from the density tokens `AppearanceService` writes on `<html>` (…); `--file-row-h` is pinned to the CDK virtual-scroll **`FILE_ROW_HEIGHT`** and must not be overridden in CSS.» `grep -rn "FILE_ROW_HEIGHT" src` is **empty**; the CDK `itemSize` is `rowHeight()` (`commit-inspector.html:309`, `changes-list.html:16`, `blame-viewer.html:94`, `file-history-panel.html:89`, `commit-list.html:46`), fed by `computeMetrics`. `screens.md:23`'s marker, written by **T59 in this wave**, says «`FILE_ROW_HEIGHT` does not exist in the repo». `sad.md` is in T59's `files_hint`, is edited by this wave (93 / 53), and T59 declared its sweep re-scoped to every tracked file the branch touches. **Failure scenario:** a maintainer reads §3's technical constraints, greps for `FILE_ROW_HEIGHT`, finds nothing, and either discards the whole constraint — including the «must not be overridden in CSS» half, which is real and which `styles.css:289-295` enforces — or invents the constant to satisfy it. That constraint is exactly the one R15-S1-F2's failure scenario A turns on: overriding `--file-row-h` in CSS misplaces every row, because it *is* the CDK's `itemSize`. **Why it matters:** same species as R15-S1-F3 and F4 — the wave fixed the site the finding named and missed the sibling, with the knowledge demonstrably in hand in the same task | **Fix now** → T63 |
| **R16-L-F2** | **Round-15 O1 was not delivered, the wave rewrote the line without fixing it, the same edit added a second false claim, and the row that pins AC-18's carve-out still visits one density.** `inspector-layout.spec.ts:369-397`, the 220 / 110 collapsed twin. Three defects, all inside lines this wave **wrote** (each is a `+` line in `git diff 805a32d`). **(1)** `:370-371` still reads «the collapsed twin of the row above, and **the one REACHABLE configuration** where AC-18's cross-placement guarantee yields». Round-15 O1 quoted that sentence, measured 688 sub-floor configurations outside the stated band alone in 12 of 21 token sets, and routed it to «ride along with F1» — F1 went to T59, which touches no `src` file, so nothing carried it. It is now *more* false than when raised: relaxed is in scope as of this round and gives 0.609091 at the same remainder. **(2)** `:401-402` adds «at a 110 px remainder the 2-row floor wins and **the two collapse states are numerically identical**» — measured at remainder 110 with the default font: comfortable **34 / 34** and relaxed **43 / 43** identical, **compact 36 / 27** not identical, with the collapsed share **0.754545** *meeting* 0.750 because the guard never binds there. The same false «alike» claim as the live `test-plan.md:90` cell (R16-S1-F1). **(3)** `:390-394` passes `COMFORTABLE_TOKENS` and asserts `headerMaxH === COMFORTABLE_TOKENS.panelHeadH` and `share ≈ 0.6909`, while T61's per-density `EXPANDED_CAP` / `EXPANDED_SHARE` maps sit **28 lines below** — the three-density treatment stops one row short of the row that pins the carve-out itself. **Failure scenario:** a maintainer reads the row documenting AC-18's carve-out, learns the guard yields in «the one reachable configuration» and that the two collapse states are identical there, and concludes the band needs no per-density treatment — the exact belief R15-S1-F1 blocked on, restated in the file T61 rebased. And because the row never runs compact, a policy change dropping the compact collapsed share below 0.750 at remainder 110 — where today it is 0.754545, inside no carve-out — would leave the suite green. **Why it matters:** point 1 is commissioned scope not delivered, point 2 is a defect this wave introduced, point 3 is the coverage gap that let both survive. Stage 1 recorded point 1 as its O1; the lead raises all three as one finding because they share a cause | **Fix now** → T64 |

## Checked and clean

**T60 is correct, and it is the wave's high point.** Three grids, three agents, one answer: the class is
closed, the behaviour after the guard is *right* and not merely defined (a body-less commit takes the
pre-T60 non-divergent path byte for byte), both halves of the pinning row are load-bearing, and every
one of the eight claims in the caveat it wrote into shipped code is true. Round 15 blocked on a caveat
that stated a false identity; this one does not.

**The fixtures are genuinely derived.** `tokensFor()` computes the tokens through `computeMetrics`
while every expected value stays a literal, so mutating the generator reddens the suite three ways
over (7 / 8 / 14 rows). A self-fulfilling fixture would have left all three green.

**Mutation coverage: 42 labelled runs across the three agents** — stage 2's 39 (**38 redden**, one
green and it is R16-S2-F2, **no unexplained green**) plus stage 1's 3. Between them: every constant,
both row floors, both share floors, the head-cap guard, all three rounding sites, the `bodyLines`
term, the share-floor ternary, the `stackedPanelsHeight` subtraction, the `protectedList` empty arm,
`Math.min(fileCount, …)`, the consumer's clamp floor, all four splitter clamps in both directions,
all four stacked bases, both inner panel hosts' `h-full`, and `computeMetrics`' own pads and ratio.
The pre-existing guards still redden after T61's re-base at the same counts round 15 measured, so the
fixture re-base weakened nothing.

**`MIN_BOTTOM_PX` is now the best-covered of the four splitter clamps**, and the last one whose
coverage was one-directional. Mutation **C4** (`main-content.ts:46`, 220 → **400**) reddens 1 / 840 —
`expected '400px' to be '580px'` in the AC-18 bottom-placement row — which, paired with round-15's
L13 (220 → 180, 1 failed), witnesses it from **both** sides. And it is witnessed *dimensionally*
rather than structurally: `main-content.spec.ts:600-606` asserts the clamped height **and** its
residue in one row (`expect(800 - parseFloat(…)).toBe(220)`). Nothing to route.

**Conventions, boundaries, security.** No new `any` / `as any` / `@ts-ignore` / `@ts-expect-error`
anywhere in the feature diff; the single `as unknown as` predates the wave and is a test stub cast. No
new `invoke(`, `innerHTML`, `outerHTML`, `eval`, `new Function`, storage access, `document.write`,
`insertAdjacentHTML`, `bypassSecurity`, IPC command, widened Tauri surface or secret — zero hits over
the whole diff. Every component carries `OnPush`; every `ResizeObserver` and listener has a disposal
(`commit-inspector.ts:389`, `:398` via `effect(onCleanup)`; `:423` via `afterNextRender` +
`destroyRef.onDestroy`). No new dead code. AC-05's «the released height stays empty» is true in CSS as
well as in the policy (`commit-inspector.css:183-185`). The NaN-from-`getComputedStyle` class was
checked for its other instances and `measureHeaderActions` is not a second hole.

**T60's and T61's honesty reproduces figure for figure.** Both name their unsatisfiable DoD bullets
with the reason; every replacement measure they offer (the per-file `expect(` counts 47→60, 45→72,
106→118, 29→33; the removed-`expect(` line being the `toBe(7)` → `toBe(9)` re-derivation; the gate's
own unescaped-grep instrument returning 3 where the escaped form returns 0) was re-run and holds.

## Observations — recorded, not findings

- **O1** T59's Outcome says the re-scoped sweep covered **167** tracked files; the union is **171**
  today. The four-file difference is the wave's own task files, so the figure is plausibly right when
  measured — but the bullet does not say when.
- **O2** `commit-inspector.ts:583-584`'s parenthetical «(or NaN, when jsdom resolves `line-height:
  normal`)» compresses two different cases: with no `#bodyText` `lineH` is 0, and the NaN arises only
  when a body *is* rendered and the property computes to a keyword. Both are covered by the guard; the
  sentence reads as if NaN were an alternative outcome of the no-body case. One-clause fix.
- **O3** T60's Outcome cites the clamp assertion at `:632:44`; in the delivered tree it is `:634:44`.
  Transcription only.
- **O4** Round-15 **O4**'s note should be upgraded from «deliberately left unguarded» to «unreachable,
  enforced at `preferences-schema.ts:304-311` and `preferences.service.ts:229-237`» — the current
  wording invites a fourth guard nobody needs.
- **O5** `DESIGN.md:347-354` states the exception as a correct **paraphrase** rather than the canonical
  300-character sentence. Deliberate and right — the sentence is longer than the surrounding prose —
  but it means the byte-identity invariant covers 10 of 12 statements of the exception, and that should
  be said where the invariant is written down so the next sweep does not read the paraphrase as drift.
- **O6** `inspector-layout.spec.ts:432-436`'s `EXPANDED_SHARE.get(tokens.panelHeadH) ?? 0` turns a
  missing map key into a comparison against 0. Mutation **M3** settles it: a key the derivation
  outgrows fails loudly («panelHeadH 37: expected 97 to be undefined»), so the `??` cannot hide a
  drift. The sibling `EXPANDED_CAP` has no `??` and gives the clearer message.
- **O7** `commit-inspector.spec.ts:479`'s premise assertion and the same row's `bodyLines: 12 /
  lineH: 0 / headerFixedH: 0` arguments are decorative. Noted so a future round does not read them as
  coverage.
- **O8** T61's `h-full` pins assert a **class**, not a computed height — inherent to a jsdom tier with
  no stylesheet, and the precedent the repo settled in R11-S2-F2. A Tailwind purge that dropped the
  `h-full` rule would leave the rows green.
- **O9** `GROWTH_UTILITIES` (`main-content.spec.ts:229`) matches `flex-[0_0_30%]`, a fixed basis rather
  than growth. No false positive today because the wrappers set their basis through `[style.flex]`;
  the regex is stricter than needed, which is the safe direction.
- **O10** The gate line `grep -rn "it.skip\|\.only(\|it\.todo\|\.concurrent" src/` returns **3** on
  this tree, all three the string `'Commit skipped.'` in `sequencer-ops.ts`; escaped, it returns **0**.
  Right conclusion, wrong instrument — worth fixing wherever the gate line is written down. Raised by
  T61 itself.
- **O11** Round-15 **O5** stands: `ARCHITECTURE.md:137-139` says row heights «are fixed by the density
  tokens in `styles.css`» against `styles.css:289-295`'s own words. It is the belief that made the
  `{30, 30}` fixture plausible.
- **O12** Round-15 **O6** stands: **AC-19** is named in exactly one `it()` / `describe()` title
  (`main-content.spec.ts:411`), measured over 943 titles. No cover is lost — T61 strengthened that row
  and its mutation reddens it — only the round-14 figure was wrong.
- **O13** Round-15 **O10** stands and is not routed: `tasks.json` claims one AC the task file does not
  for **T44** (`AC-02`) and **T49** (`AC-01`). Re-measured mechanically over all 62 by the lead: still
  exactly those two. Reserved for the single later pass round-13 O15 and round-14 O12 named. Stage 1
  could not reconcile T62's `P1 = 12` / `P2 = 16` sets under a looser reading of the predicate (its own
  count is 31, a superset containing all 16); the difference is presumably T55's docs-task exclusion.
- **O14 — commit hygiene, for `ship`.** `.github/workflows/release.yml` (+39), `install.sh` (+14 / −11),
  `.gitignore` (+3), `.mcp.json`, `CHANGELOG.md`, `CONTEXT.md` and `DESIGN.md`'s non-feature hunks sit
  in the same working tree and no task file, `tasks.json` entry or `files_hint` claims the first four.
  Reviewed anyway: the new release job scopes `permissions: contents: write` to itself, uses
  `secrets.GITHUB_TOKEN` rather than a PAT, and its `grep -q api.github.com` guard fails closed;
  `install.sh` replaces an unauthenticated `api.github.com` call with the TLS redirect off github.com.
  No security concern — but `git commit -a` on this branch would sweep all of them into the feature
  commit.
- **O15 — half of `inspector-layout.spec.ts`'s assertions are not evidence about the fixtures, and
  T64 will be editing that file.** The spec now mixes two kinds. **Derivation-anchored** —
  `expect(result.headerMaxH).toBe(COMFORTABLE_TOKENS.panelHeadH)`, `EXPANDED_CAP.get(tokens.panelHeadH)`
  — move *with* `computeMetrics` and prove nothing on their own; under mutation M2 the message
  `expected 34 to be 40` is one of these, where the **expectation** moved rather than the received
  value. **Literal-anchored** — the majority, and the actual coupling that makes the derivation real
  (`:74`'s `toBe(9)`, `:169`'s `toBe(606)`, the `56` in the squeezed AC-03 row). The conclusion that
  the fixtures are not self-fulfilling rests on the second kind only. Worth stating where the
  derivation is documented, so a future round does not read a derivation-anchored redden as proof of
  coupling — and so T64 does not replace a literal with a derived expression and quietly remove the
  coupling while the suite stays green.

## Verdict

**CHANGES REQUESTED.** Seven stage-1 findings, two stage-2 findings, two lead findings. All eleven
resolved with the owner as **Fix now**; nothing deferred, nothing dismissed.

**What the wave executed, it executed well.** T60 is the first production fix this branch has taken
from a review since T44, and it is correct, load-bearing and correctly caveated. The fixtures now come
from the generator and the coupling is real. The closed-form band is exact for what the criteria
promise, at 201 474 independently measured points. Four constants that took any value with the suite
green now have witnesses in both directions. 38 of 39 mutations redden, and the one green is a finding
rather than an unexplained hole.

**What blocks is what blocked in rounds 12–15, and the shape has not changed:** the fix propagated
correctly and the sweep that was supposed to find every carrier stopped one artefact short — this time
inside `src/` (F2, L-F2), inside the test plan's own counts (F3, falsified by the wave's own code
change), inside the three cells a tester executes for AC-18 (F1), inside a SAD constraint naming a
constant the same task recorded as non-existent (L-F1), and inside the record's own verification
command (F4, seventh consecutive round). F5 is commissioned scope, named site-by-site in the round-15
verdict, delivered at three sites of sixteen. **The chain itself is intact:** 8/8 user stories and
22/22 acceptance criteria trace end to end, 21 of 22 to automated tests.

**Owner decision on the recurrence, taken this round.** Five rounds of prose sweeps have each found
real carriers and each stopped short. The strategy changes: **replace every per-density figure in the
artefacts with the closed form in `panelHeadH` / `fileRowH`, so no density can go stale**, and then
**add a mechanical gate check that recomputes whatever figures remain from the generator and fails on
drift**. Eliminating the class beats detecting it, and detecting what is left beats sweeping for it.

Routed to four tasks in the branch's standing order:

| task | scope |
|---|---|
| **T63** — round-16 criteria amendment (docs, `deps: []`) | **R16-S2-F1**: reword the predicate at the ten byte-identical sites to «costs the list its ratio», and restate `spec.md:198`'s certification as the share-yield predicate it measured · **R16-S1-F1**: `test-plan.md:90`, `:91`, `:167` — the real per-density figures, and drop the false «one panel head / expanded and collapsed alike» claim · **R16-S1-F3**: express the sweep counts as the loop's own arithmetic (288 / 576 / 48 today) at `:35`, `:99`, `:145-151` · **R16-S1-F5**: the three shipped densities at the thirteen remaining live sites, and `:165`'s «4 runs» becomes 6 · **R16-L-F1**: `sad.md:38` names what the CDK `itemSize` actually reads · **O1**, **O5** · replace every per-density figure with the closed form per the owner decision · register T63–T66 |
| **T64** — round-16 test coverage (domain, `deps: [T63]`) | **R16-S2-F2**: one unit row derived from the fixture, at a height whose quotient has a fractional part ≥ 0.5 inside `[1, MAX_CLAMP_LINES)`, asserting `clampLines` · **R16-S1-F2 / R16-S2-F3**: the seven stale figures at `inspector-layout.spec.ts:547-572` replaced by the closed forms, with the `relaxed` column · **R16-L-F2**: delete the «one REACHABLE configuration» claim, correct the «numerically identical» claim, and run the 220 / 110 carve-out twin across all three densities · **R16-S1-F7 (test half)**: `:590`'s title says «floors» · **O6**, **O7** · **O15**: when replacing the per-density figures with closed forms, keep the literal-anchored expectations literal — `:74`'s `toBe(9)`, `:169`'s `toBe(606)` and the AC-03 row's `56` are the whole of what couples the suite to `computeMetrics`, and turning them into derived expressions would remove the coupling with the suite still green |
| **T65** — round-16 figure gate (infra, `deps: [T63, T64]`) | The mechanical check the owner chose: a script that recomputes every figure the artefacts still quote from `computeMetrics` and the shipped policy, and fails the gate when one drifts. Covers the class R10-S1-F10 → R16-S1-F3 has recurred in for six rounds. Also fix the gate's own unescaped-grep instrument (**O10**) |
| **T66** — round-16 records (docs, `deps: [T64, T65]`) | **R16-S1-F4**: cite `_epic.md`'s invariant by its bold anchor **everywhere, including inside explanatory parentheses**, and correct T62's Outcome to what its two commands return · **R16-S1-F6**: un-nest the comment at `test-plan.md:113` · **R16-S1-F7 (record half)**: the enumeration lists the four carriers it names, drops the declared non-carrier, corrects `:80-83` → `:84-90`, and **names the scope it swept** · **O3**, **O4** |

No open stage-1 finding remains unresolved. **T60 changed shipped behaviour and was right; nothing in
this round asks for another production change** — R16-S2-F2's `Math.floor` is already what ships, and
what is missing is its witness.
