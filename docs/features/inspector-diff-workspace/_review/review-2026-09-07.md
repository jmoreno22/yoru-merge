---
slug: inspector-diff-workspace
date: "2026-09-07"
round: 6
verdict: CHANGES REQUESTED
feature_size: "M"
route: standard
branch: feat/inspector-diff-workspace
base: 7cd47b4
head: ba8fea0
previous_review: review-2026-09-04-round5.md (CHANGES REQUESTED, V1–V7)
reviewers: 2 × sdd:reviewer (clean context, read-only, model opus, effort high) — stage 1 closure of V1–V5 + claimed ACs + full chain trace; stage 2 quality of the changed surface. Reports written to scratchpad files first, then relayed (the idle message truncates). The stage-1 reviewer closed V1–V3 by running the mutation (restoring the tree afterwards) rather than by reading; the lead independently recomputed the 126 literal, traced every `WORKING_SIDES` reader and checked the inspector template for focusables.
---

# Re-review round 6 — inspector-diff-workspace — 2026-09-07

## Scope

Sixth review of the branch, after T31 (the round-5 follow-ups). Whole feature diff `7cd47b4..ba8fea0`: 38 commits, 122 files. Changed surface since round 5 (`7601d2b..ba8fea0`): `f6ec0b5` (docs: round-5 record + T31 task) and `ba8fea0` (T31, `test`: the `focusKeyOwner` row, the `WORKING_SIDES` retype, the `126` literal plus the hoisted injection, the swapped focus assertion), 11 files, +184 / −6 — 4 under `src/`, nothing under `src-tauri/`. `SDD-AC` trailers claim AC-03, AC-08.

Gate at review time, run by the lead on `ba8fea0` and re-run independently by the stage-1 reviewer: `pnpm test` 831 tests / 63 files green (830 in round 5; the one new test is the `focusKeyOwner` row) · `pnpm lint` (biome, 266 files) clean · `pnpm build` clean (906.71 kB main, under the 1.2 MB budget) · `tsc --noEmit` clean. Rust gate unchanged (no `src-tauri` change). The one stderr line (`[cdkFocusInitial]` not focusable, from the AC-10 row) is the known jsdom noise.

Out of scope, noted so `ship` is not surprised: the working tree carries four modifications unrelated to this feature (`.github/workflows/release.yml`, `.gitignore`, `CHANGELOG.md`, `install.sh`) plus an untracked `.mcp.json`. Neither reviewer touched them and they are not part of the feature diff.

## Resolution of the round-5 findings

| Finding | Resolved | Evidence at HEAD | Mutation that turns the pin red |
|---|---|---|---|
| V1 `focusKeyOwner` has no direct row | yes | `diff-workspace-state.spec.ts:327-346` — one `toEqual` naming all four keys, each built through `commitRowKey` / `focusKeyFor` rather than restated as a literal | executed: deleting the `working-tree` arm (`diff-workspace-state.ts:82`) leaves 1 failed / 27 passed, and the failure is this row |
| V2 `WORKING_SIDES` restates the source union | yes | `diff-workspace-state.ts:64-65` — `type WorkingSide = Extract<DiffWorkspaceSource, { kind: 'working-tree' }>['side']` | executed: renaming the side in the union gives `diff-workspace-state.ts(65,58): error TS2820`. No runtime change — one private constant, one reader (`:82`), which only interpolates the element into a template literal. See S2 for what it does *not* catch |
| V3 the host row pins equality, not the value | yes | `main-content.spec.ts:338` — `expect(expected.headerMaxH).toBe(126)` beside the equality at `:336` | executed: with the policy shifted by −1 the equality at `:336` stays green and only `:338` fails (`expected 125 to be 126`). The lead recomputed 126 independently: `remainder` 560, `floor` 280, `fileListHeight` 34 + 4 × 30 = 154 → `round(max(126, 34))` = 126 against `inspector-layout.ts:96-98`, tokens 30 / 34 (`appearance-metrics.spec.ts:20,24`) |
| V4 one vacuous assertion in the Q2 row | **no** | `commit-inspector.spec.ts:742` | none — see F1 |
| V5 `AppearanceService` injected twice | yes | `main-content.spec.ts:306`, reused at `:314` and `:331` | n/a — a hoist; no assertion touched |

**The V4 replacement, traced.** The expiry lives in the `index < 0` branch of the inspector's restore effect (`commit-inspector.ts:337-348`): it calls `focusRestored(key)` and then `return`s, never focusing anything. The only `.focus()` on the restore path is `focusVirtualRow` (`virtual-row-focus.ts`), unreachable while `index < 0`, and neither `close()` nor `focusRestored()` moves focus (`diff-workspace.service.ts:255-266`); the `[tabindex]` ancestor round 5's V6 mentions is read at *open* time by `focusedRowKey()` (`:283-294`), never written at restore time. So `host.contains(document.activeElement)` is `false` in both branches. The stage-1 reviewer proved it rather than arguing it: with the expiry removed the row fails at `:738`, and a temporary probe kept alongside that same mutation reported `{ onBody: true, inHost: false }` with the key unexpired — 28 passed. Both files were restored with `git checkout --`.

**Claimed ACs.** AC-03 genuine — the host writes the policy's cap and the value is now pinned on both the host row and the pure table (116 / 120). AC-08 genuine — the shared owner predicate is pinned on its three arms, and the row that proves an expired key does not steal the focus remains revert-sensitive through `commit-inspector.spec.ts:738` and the returning-path block at `:750-755`. F1 costs a claim in two documents, not an acceptance criterion.

## Chain trace

**User stories.** US-01..US-08 each keep ≥ 1 AC and a `sad.md` §6 flow (unchanged).

**Acceptance criteria.** Every AC-01..AC-21 reaches code and at least one automated assertion across every surface `sad.md` `target_surfaces` declares, except AC-20 — still the only manual exception, its owner decision recorded at `sad.md:633`, `test-plan.md:87` and the earlier review records. `tasks.json` T31 equals `tasks/round5-fixes.md` in acs, deps and all six `files_hint` entries; `tracker.md` has T31 `done`, T26 `blocked`; every file in `ba8fea0` is inside `files_hint`; no `.skip` / `.only` / `.todo` / `xit` under `src/`; exactly one assertion was removed by `ba8fea0` — the vacuous V4 line it replaced — and none was loosened.

**Focus-key ripple (unchanged from round 5, re-verified).** `commit-list.ts:271-272,300`, `working-changes/changes-list.ts:112-128`, `commit-inspector.ts:332-352`, `diff-workspace.service.ts:285-294`, `diff-workspace-state.ts:58`. Module boundary: `diff-workspace-state.ts` imports nothing and `features → core/services` runs downwards (`ARCHITECTURE.md:110`).

## Findings — stage 1

- **F1 V4 is not closed: the Q2 row still carries an assertion that cannot fail** — `commit-inspector.spec.ts:742`; AC-08; T31 DoD bullet 3. T31 swapped `expect(activeRowPath(host)).toBeNull()` (null because the emptied list draws no rows) for `expect(host.contains(document.activeElement)).toBe(false)` (false because nothing on the restore path focuses inside the panel), and rewrote the comment to call the new line «the failable half». Proven by mutation, above. No AC is violated — the row stays revert-sensitive through `:738` and `:750-755` — but `test-plan.md:111` and the `ba8fea0` commit body report the item fixed. → **Fix now → T32** (drop the line; correct both documents).

No other stage-1 finding: V1, V2, V3 and V5 are closed with the concrete mutation that reddens each new pin, the claimed ACs are genuine on traced paths, and the spec → §6 → screens → tasks → code+test chain is intact.

## Findings — stage 2

- **S1** is the same defect as F1, reached independently from the conventions side: the new comment claims a «panel's focusable fallback» and the line is not the failable half. Its supporting claim that the host has no focusable element is imprecise — `commit-inspector.html` has seven native `<button>`s, so the line is a live guard against focus landing on a panel control — but that does not rescue it: nothing on the restore path puts focus there, so it cannot fail for the mutation it is advertised against, and the comment overstates what it proves. → folded into **T32**.
- **S2 `WORKING_SIDES` still compiles if the source union gains a side** — `diff-workspace-state.ts:63-65` against `:4`; AC: n/a. `readonly WorkingSide[]` catches a *rename* (what T31's DoD asked, and the rename also breaks `file-row.ts:43` and `working-changes.ts:256`, which feed `DiffWorkspaceSource` directly) but not an *addition*: a third `working-tree` side widens `Extract<…>['side']` while the two-element array still type-checks, and `focusKeyOwner('conflicted:src/a.ts')` would return `'commit-file'`. The reviewer's `as const satisfies` suggestion (the repo idiom at `preferences.service.ts:88`) does not change that guarantee either — only an exhaustive `Record<WorkingSide, string>` would, and that is speculative for a two-member union that has been stable. → **Not an issue** (owner; the DoD asked rename-safety and has it — recorded here as a known limit).
- **S3 the new alias duplicates an exported `WorkingSide` of identical meaning** — `diff-workspace-state.ts:64` vs `features/working-changes/file-row.ts:39`, which still spells the union inline at `:4`, `:16`, `:48`, `:172`. The local alias was forced: core cannot import from features. Unifying means exporting the core alias and editing `file-row.ts` and its four inline unions — files outside T31's scope. No boundary is violated. → **Not an issue** (owner; adjacent refactor, recorded as accepted duplication).
- **S4 the `126` literal also pins the default density tokens** — `main-content.spec.ts:330-338`; AC-03. `expected` is computed from live `appearance.fileRowHeight()` / `panelHeadHeight()`, so a future density change breaks an AC-03 row with a bare number; the derivation comment at `:337` is what will explain it, and it does. This is the cost V3 asked for. → **Not an issue** (accepted cost, recorded so the next density change knows where to look).
- **Docs nit, no task:** `tasks/round4-fixes.md:61` now describes the mutation that really applies (the round-5 V3 misreport is gone) but still opens «3 false-pass pins verified by mutation» while the first of the three is explicitly not mutation-verified. Sentence and count no longer agree.

Checked and clean (stage 2): the `toEqual` shape at `diff-workspace-state.spec.ts:327-346` is the file's idiom, reports all four arms in one diff, and building the keys through the producers pins producer + classifier as a round trip; the row's placement inside `describe('close', …)` matches its sibling at `:303-322` and T31 allowed it; `Extract<DiffWorkspaceSource, { kind: 'working-tree' }>['side']` is correct and minimal, with no lost narrowing at the single use site; no `as any`, no non-null `!`, no cast; biome clean on all four files and `tsconfig` strictness needs no suppression; the two other new comments carry a real *why*; no `src/testing/` helper re-implemented and the V5 hoist removed the only duplicated call; no new cross-module import, no new DOM sink, no secret, no widened Tauri surface; `test-plan.md:110` matches what the V1 / V2 row asserts.

## Owner decisions (2026-09-07, round 6)

1. **F1 / S1:** fix now, one task — T32 «Round-6 fixes: drop the unfailable focus line from the Q2 row and correct the two documents that report V4 closed». The alternative (keep the line with a corrected comment) was declined: the line's only value is as a forward guard, and T31's own task file already allowed the removal.
2. **S2:** not an issue — the DoD asked rename-safety and has it; the only form that would catch an addition is speculative for a stable two-member union. Recorded above as a known limit, no spec §8 entry.
3. **S3:** not an issue — the duplication is forced by the module direction and unifying it is an adjacent refactor outside T31's scope. Recorded above as accepted duplication.
4. **S4** and the `round4-fixes.md:61` count nit: no action.

## Artifacts changed by this review

- `tasks/round6-fixes.md` (T32) written; `tasks.json`, `tasks/tracker.md`, `tasks/_epic.md`: T32 added (`todo`); T26 stays `blocked`.
- `test-plan.md` is **not** touched here: its `:111` row must change to match what T32 lands, so the correction belongs to the task rather than to this record.

## Gate result

**CHANGES REQUESTED** — stage 2 in substance, and only because a closure was over-reported. Four of the five round-5 findings are closed with an executed mutation behind each pin, AC-03 and AC-08 are genuine on traced paths, the 126 literal is arithmetically right, the chain is intact and the gate is green including `tsc --noEmit`. What remains is F1: one assertion that passes in both branches of the expiry, and two documents that call it fixed. Re-review the changed surface after `/sdd:implement inspector-diff-workspace` runs T32; T26's screenshots, the R15 Linux check and the manual checklist stay owner-only prerequisites for `ship`.
