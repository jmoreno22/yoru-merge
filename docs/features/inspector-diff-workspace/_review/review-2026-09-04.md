---
slug: inspector-diff-workspace
date: "2026-09-04"
verdict: CHANGES REQUESTED
feature_size: "M"
route: standard
branch: feat/inspector-diff-workspace
base: 7cd47b4
head: f3069c2
previous_review: review-2026-09-03.md (CHANGES REQUESTED, F-1..F-23)
reviewers: 4 × sdd:reviewer (clean context, read-only, model opus, effort high) — stage 1 F-1..F-9 + AC-01..AC-10, stage 1 AC-11..AC-21 + whole-chain backstop, stage 2 changed surface (two passes; the first was cut off after 9 of 17 findings and a gap pass re-covered the remaining dimensions)
---

# Re-review — inspector-diff-workspace — 2026-09-04

## Scope

Second review of the branch after the follow-ups of the 2026-09-03 review. Whole feature diff `7cd47b4..f3069c2`: 29 commits, 110 files, +9 222 / −325. Changed surface since the first review (`ae737e4..f3069c2`): the docs commit `9f4bae4` plus 11 task commits T15–T25 (T26 delivered its boundary notes only and is `blocked`), 77 files, +4 049 / −446, no file under `src-tauri/`. `SDD-AC` trailers on the follow-ups claim AC-01..AC-08, AC-10..AC-18; the review re-traced the whole §4 / §5 set.

Gate at review time, run by the lead on `f3069c2`: `pnpm test` 801 tests / 61 files green (two tiers under `ng test --no-watch`; the commit-inspector spec logs «No icon named lucideX was found» to stderr — see D6) · `pnpm lint` (biome, 262 files) clean · `pnpm build` clean (906 kB main, under the 1.2 MB budget). Rust gate unchanged since the first review (no `src-tauri` change).

## Resolution of the 2026-09-03 findings

| Finding | Resolved | Evidence | Note |
|---|---|---|---|
| F-1 Esc wipes the commit draft | yes | `escape-layers.ts:70-74`, `escape-layers.service.ts:74-80`; opt-in only on the four filters | Asserted pure-TS (`escape-layers.spec.ts:110-183`) and rendered (`working-changes.spec.ts:294-312`) |
| F-2 no 75 % floor | yes | `inspector-layout.ts:30,70-73`; `inspector-layout.spec.ts:197-337` | Tabled over 6 / 30 files; the row fails without the floor |
| F-3 three shortcuts missing from Settings | yes | `diff-workspace.service.ts:116-169`; `settings-dialog.spec.ts:75-95` | Six rows with the workspace closed; Close registered with `when: () => false` |
| F-4 watcher never reloads the diff | yes | `repo-ops.ts:126-134`; `working-changes.spec.ts:232-246` | Test drives `refreshAll()`, not the watcher listener (G9) |
| F-5 external emptying advances; wall clock | yes (caveat) | `diff-workspace-state.ts:92-116`; `repo-ops.ts:290`; `working-changes.spec.ts:248-260` | Clock gone; origin read from `stagingBusy` at load start — the interleaving caveat became D2 |
| F-6 focus restore incomplete | yes (partial coverage) | `virtual-row-focus.ts:27-43`; `commit-list.ts:213-224`; `diff-workspace.service.ts:274-285` | Scroll precedes focus, restore inside the list's own `afterNextRender`, commit-row branch reachable; the commit-list side has no rendered test (G5) |
| F-7 AC-07 / AC-17 collision | yes | `diff-workspace.service.ts:237-246`; `diff-workspace.spec.ts:219-242` | `screens.md` still carries the pre-amendment state (G10) |
| F-8 uncapped header, dead CSS cap | yes | `commit-inspector.ts:538-543`; `commit-inspector.css:1-3` | Measured from the laid-out box; the cap is gone and nothing bounds the header any more (D5) |
| F-9 component tier never enabled | yes | `angular.json` `test` target; `vitest.config.ts:21-43`; nine `jsdom` specs | Real `TestBed`, real services, only `invoke` stubbed; no `.skip` / `.todo` under `src/` |
| F-10 filter keystroke yanks the workspace | yes, code only | `commit-inspector.ts:307-317` | No assertion at any level (G7) |
| F-11 double fetch | yes, code only | `commit-inspector.ts:667-676`; `working-changes.ts:294-300`; `diff-workspace.service.ts:287-310` | No test counts the fetches (C5) |
| F-12 `navigated` on advance | yes | `diff-workspace-state.ts:113`; spec `:150-163` | |
| F-13 core → shared barrel | yes | `diff-workspace.service.ts:2-4` | Path import; the dependency direction itself is undocumented (D16) |
| F-14 global `document` | yes | `diff-workspace.service.ts:55,276-284` | |
| F-15 `panelPad` | yes | `inspector-layout.ts:2-7` | |
| F-16 `snapshot.activeFile` / `shownFile()` | yes | `diff-workspace-state.ts:12-19`; `activeCommitFile` read at `diff-workspace.service.ts:299` | |
| F-17 composer hidden three ways | yes | `main-content.html:63-64` | One `hidden` class on the still-mounted panel |
| F-18 Esc matrix empty field | yes | `escape-layers.spec.ts:110-136,155-183` | |
| F-19 strip duplicated | yes | `shared/ui/yoru-diff-source.ts`; both hosts; hints from `KeyboardShortcutsService.hint` | |
| F-20 DESIGN.md rows bound | yes | `DESIGN.md:343,395` | |
| F-21 undeclared files, T8 / T10 / T14 | yes | `tasks/collapsed-header-actions.md:37`, `centre-and-portal-wiring.md:41`, `docs-design-shell.md:36` | Seven follow-up tasks widened again without a note (G12) |
| F-22 screenshots | partial, by DoD | `tasks/tracker.md:37` | T26 `blocked` on the owner; `docs/screenshots/` untouched since the base |
| F-23 tooltip Esc | dismissed 2026-09-03 | — | No regression |

## Chain trace

**User stories.** US-01..US-08 each keep ≥ 1 AC and a `sad.md` §6 flow (unchanged from the first review).

**Acceptance criteria.** Every AC-01..AC-21 reaches code and at least one automated assertion, except AC-20, which is on the manual release checklist by owner decision. The four ACs the first review found with zero automated coverage (AC-14, AC-15, AC-18, AC-21) all have component tests now. The AC-07 amendment is coherent across `spec.md:131`, `sad.md:374,409,620`, `ux-flows.md:94,97,206`, `test-plan.md` and T21's task file; `screens.md:154,366` was not in T21's `files_hint` and still describes the pre-amendment state (fixed by this review).

| AC | Verdict | Remaining gap |
|---|---|---|
| AC-01 | OK (caveat) | header metadata + six actions unasserted (G3); built-app share manual |
| AC-02 | OK (caveat) | toggle shortcuts never dispatched (G4); restart persistence manual |
| AC-03 | OK (caveat) | squeezed-column component row missing (G2); header no longer bounded (D5) |
| AC-04 | OK | rows past six while open untested (G6) |
| AC-05 | OK | |
| AC-06 | OK (caveat) | rows past six (G6); fetch count (C5); 150 ms / single-load manual |
| AC-07 | OK | residual path leaves `activeCommitFile` set (D4) |
| AC-08 | OK (caveat) | commit-list side has no rendered test (G5); pending key never expires (D3) |
| AC-09 | OK (caveat) | «at most one workspace» structural, never counted |
| AC-10 | OK (caveat) | five-layer rendered row missing (G1) |
| AC-11 | OK (caveat) | filter guard has no assertion (G7) |
| AC-12 | OK (caveat) | hunk keys inside the workspace untested (G8); toggles fire in Changes (C2) |
| AC-13 | OK | origin race (D2); test-plan wording (G11) |
| AC-14 | OK | |
| AC-15 | OK | |
| AC-16 | OK (caveat) | test drives `refreshAll()` (G9); refresh race (D1) |
| AC-17 | OK | |
| AC-18 | OK | height vs 1.0.5 manual |
| AC-19 | OK (caveat) | rendered «only the diff grows» manual by plan |
| AC-20 | OK | manual by owner decision |
| AC-21 | OK | |

## Findings — stage 1

None is a behavioural violation of an AC. G1–G9 are rows `test-plan.md` declares `component · automated` that were never written, or fixes with no assertion; G10–G12 are artifact drift.

- **G1 AC-10 five-layer Esc row never implemented** — `test-plan.md:68`; the only rendered Esc press is `working-changes.spec.ts:305`; the 64-combination sweep proves the pure function, not that consumers register at their rank. → **Fix now → T27.**
- **G2 AC-03 squeezed-column row does not exist** — `test-plan.md:44`; `--inspector-clamp-lines` is in no assertion. → **Fix now → T27.**
- **G3 AC-01 expanded header metadata and six actions unasserted** — `commit-inspector.spec.ts:210-254` vs `test-plan.md:36`; `commit-inspector.html:109-152` uncovered. → **Fix now → T27.**
- **G4 AC-02 toggle shortcuts never pressed** — `diff-workspace.service.ts:148-169`; `spec.md:100`, `test-plan.md:40`; only the ids are asserted. → **Fix now → T27.**
- **G5 AC-08 commit-list half of the restore has no rendered test** — `commit-list.ts:213-224,258-272`; `test-plan.md:62-63`; the inspector spec's `mod+d` row hits only the `?? path` fallback. → **Fix now → T27.**
- **G6 AC-04 / AC-06 rows past six while open untested** — `commit-inspector.ts:565-574`; `DESIGN.md:343`; component-only branch `inspector-layout.spec.ts` cannot reach. → **Fix now → T27.**
- **G7 AC-11 filter guard (the F-10 fix) has no assertion** — `commit-inspector.ts:311-317`. → **Fix now → T27.**
- **G8 AC-12 hunk-navigation row never written** — `test-plan.md:72`; `diff-viewer.ts:174`. → **Fix now → T27.**
- **G9 AC-16 test substitutes `refreshAll()` for the watcher event** — `working-changes.spec.ts:107,242` vs `test-plan.md:79`; listener, debounce and echo suppression at `current-repo.service.ts:870-873` unasserted. → **Fix now → T27** (drive the stub's emitter, or amend the row to name the manual link).
- **G10 `screens.md` SCR-02 still describes the pre-amendment AC-07** — `screens.md:154`, map `:366`. → **Fixed by this review** (state retitled «error · file list emptied», AC-17 pointer added).
- **G11 `test-plan.md` AC-13 row says «absent from the DOM»** — `test-plan.md:75` vs `main-content.html:64`, `working-changes.spec.ts:112-116`; T23 settled on one `hidden` class on a mounted panel. → **Fixed by this review** (row reworded).
- **G12 seven follow-up commits widened their task boundary without a note** — T15 `tsconfig.app.json`; T17 `commit-inspector.html`; T18 `diff-workspace.service.spec.ts`, `tauri-git-stub.ts`; T19 `current-repo.service.ts`, `commit-inspector.ts`; T20 `changes-list.ts`, `virtual-row-focus.ts`, `shared/ui/index.ts`; T21 `diff-workspace.service.spec.ts`; T23 `changes-list.ts`, `working-changes.ts`. Each additive and defensible (T22 had already recorded its own). → **Fixed by this review** («Files beyond `files_hint`» note appended to the seven task files).

**T26 remains `blocked`** on the owner: `docs/screenshots/history-dark.png` and `changes-light.png` are not recaptured, `diff-workspace-dark.png` does not exist, `README.md:233-236` adds the shortcut rows without an image. No AC depends on it; T14's and T26's DoD do. It gates `ship`, not this review.

## Findings — stage 2

Behavioural edges, lead-verified against the files:

- **D1 (edge) `refreshActiveDiff` writes `diffText` unconditionally after its `await`** — `repo-ops.ts:210-222`; AC-16; the sibling `loadDiff` re-checks `isSelectedFile` (`:200`). Pre-existing body, made reachable by T19's watcher caller. → **Fix now → T28.**
- **D2 (edge) `changesOrigin` is written before the `await` from `stagingBusy()` at load start** — `repo-ops.ts:290`; AC-13, AC-16; an interleaved staging refresh and watcher `refreshAll` can publish under each other's origin (reviewer A's F-5 caveat). → **Fix now → T28.**
- **D3 (edge) `pendingFocus` never expires when no list claims it** — `diff-workspace.service.ts:250-256`; `changes-list.ts:112-122` returns when the row is absent; a later reappearance of the path steals focus. AC-08. → **Fix now → T28.**
- **D4 (edge) the residual AC-07 path clears `diffText` but not `activeCommitFile`** — `diff-workspace.service.ts:245` vs the guard at `commit-inspector.ts:672` (reset only on sha change, `:279`); the same file cannot be reloaded after the list repopulates. → **Fix now → T28.**
- **D5 (edge) the header is content-sized in a `flex-none` wrapper with no bound** — `commit-inspector.css:1-10`, `main-content.html:112`, refs `flex-wrap` at `commit-inspector.html:174`; a many-tag commit at 960 × 640 can starve the `flex-1` diff slot below the AC-03 floor. Trade-off of the F-8 fix. → **Fix now → T28.**
- **C2 (edge) the two inspector toggles fire in the Changes view** — `diff-workspace.service.ts:148-169` `when: commitDetails() !== null` vs `main-content.html:111` (inspector unmounted); silent preference flip. AC-12. → **Fix now → T28.**
- **D12 (edge, PLAUSIBLE) a restore can be dropped when the viewport's cached height is 0** — `virtual-row-focus.ts:30-42`; `working-changes.ts:241-247` remeasures only after render; AC-08. Not confirmed under jsdom; the manual close-from-Changes check covers it. → **Fix now → T28** (remeasure first or bound the wait).

Test adequacy and hygiene:

- **D6 (test) no component spec provides the icon set** — `commit-inspector.spec.ts:99-115`; every `ng-icon` renders empty and jsdom logs the missing-icon warning (visible in the gate). → **Fix now → T27.**
- **D8 (test) the hunk-collision row asserts against combos it hardcodes** — `settings-dialog.spec.ts:28-31`; AC-12. → **Fix now → T27.**
- **D11 (test) origin faked by writing `stagingBusy` directly** — `working-changes.spec.ts:100-108`; AC-13. → **Fix now → T27.**
- **C5 (test) no assertion counts fetches on the double-click path** — `commit-inspector.spec.ts:394-404`; AC-06 / F-11. → **Fix now → T27.**
- **D13 (reuse) the inspector spec builds its own `CommitDetails` fixture** — `commit-inspector.spec.ts:41-77` vs `repo-fixtures.ts:73-107`. → **Fix now → T27.**
- **C3 (dead-code) `ng_on_destroy: undefined` stub entries are unreachable** — `tauri-git-stub.ts:23` short-circuits `ngOnDestroy`; `repo-fixtures.ts:128-138`, `commit-inspector.spec.ts:108`, `settings-dialog.spec.ts:58`, `diff-workspace.service.spec.ts:31,81`; three comments claim the opposite. → **Fix now → T27.**
- **D14 (dead-code) `TEST_CONFIG` export read only in its own file; `DISPLAYED` is a copy of `PATHS`** — `repo-fixtures.ts:53`, `commit-inspector.spec.ts:371-373`. The claim that `commitFile`'s `overrides` is never passed was not verified. → **Fix now → T27.**
- **C9 (dead-code) `DiffSourceSide` exported from the barrel, imported nowhere** — `shared/ui/index.ts:31`. → **Fix now → T28.**

Docs and conventions:

- **C6 (docs) `DESIGN.md:887` rank-4 row predates the opt-in** («text field with content — built in, nothing registers it»). → **Fix now → T28.**
- **C7 (docs) `DESIGN.md:346-352` prose documents only the 50 % floor.** → **Fix now → T28.**
- **C8 (docs) `AGENTS.md:72` names two of the four `src/testing/` stand-ins.** → **Fix now → T28.**
- **D15 (docs) `focusVirtualRow` is exported from the barrel and absent from `shared/ui/README.md:267-272`.** → **Fix now → T28.**
- **D16 (convention) `core` depends on `shared/ui` by a cycle-dodging path import and `ARCHITECTURE.md` §Layers records no such direction** — `diff-workspace.service.ts:2-4`. → **Owner decision: record the exception** in `ARCHITECTURE.md` (core may import template-less services from `shared/ui` by module path, never the barrel) → **T28.**

Dismissed with reason:

- **C1 (edge) a filter keystroke or folder collapse in the Changes panel would close the workspace with the external notice** — `working-changes.ts:225-237`. → **Not an issue.** The whole panel is `hidden` while the workspace is open (`main-content.html:64`), the filter input lives inside it and no shortcut focuses it, so neither the filter nor a folder state can change in that window.
- **D7 (test) `Element.prototype.scrollTo` patched without teardown** — `diff-workspace.spec.ts:117`, `working-changes.spec.ts:163`, `main-content.spec.ts:131`. → **Not an issue.** Vitest isolates each spec file (the config sets no `isolate: false`), so the patch cannot leak across files; a shared stand-in would be tidier but is not a correctness gap.
- **D9, D10** duplicate G5 and G6.

## Owner decisions (2026-09-04)

1. **Stage-1 coverage gaps G1–G9:** fix now, one task — T27 «Component rows the plan declared». Consistent with the 2026-09-03 decision to honour the plan rather than relabel it.
2. **Stage-2 behavioural edges D1–D5, D12, C2:** fix now, one task — T28 «Edge fixes and repo docs», the bounded header included.
3. **Artifact drift and hygiene:** the SDD artifacts (`screens.md`, `test-plan.md`, the seven task-file notes) are corrected by this review; test nits go to T27, repo-doc and barrel nits to T28.
4. **D16 core → shared/ui:** record the exception in `ARCHITECTURE.md` (T28) rather than move the service.
5. C1 and D7 dismissed with the reasons above.

## Artifacts changed by this review

- `screens.md` §SCR-02: state «error · commit removed» → «error · file list emptied» with the AC-17 pointer; AC-07 map row updated.
- `test-plan.md`: AC-13 row reworded (hidden, not unmounted); re-review rows appended for T27 / T28.
- `tasks/component-test-tier.md`, `escape-opt-in-fields.md`, `root-registered-shortcuts.md`, `watcher-refresh-and-external-close.md`, `focus-restore-on-close.md`, `reconcile-ac07-ac17.md`, `strip-reuse-and-cleanups.md`: «Files beyond `files_hint`» note appended.
- `tasks.json`, `tasks/tracker.md`, `tasks/_epic.md`: T27, T28 added (`todo`); T26 stays `blocked`.
- `tasks/component-rows-declared.md` (T27), `tasks/edge-fixes-and-repo-docs.md` (T28) written.
- `spec.md` unchanged (nothing deferred to §8).

## Gate result

**CHANGES REQUESTED.** The feature does what the spec says on every traced path and every prior finding is resolved; what remains is declared automated evidence that was never written (T27) and seven narrow edges in the follow-up code (T28). Re-review the changed surface after `/sdd:implement inspector-diff-workspace` runs T27–T28; T26's screenshots stay an owner-only prerequisite for `ship`.
